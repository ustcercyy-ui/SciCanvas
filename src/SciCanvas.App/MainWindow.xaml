<Window x:Class="SciCanvas.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding ProjectDisplayName, StringFormat={}{0} · SciCanvas}"
        Width="1360"
        Height="860"
        MinWidth="720"
        MinHeight="480"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Closing="MainWindow_OnClosing"
        Loaded="MainWindow_OnLoaded"
        PreviewKeyDown="MainWindow_OnPreviewKeyDown"
        Background="#0D1117"
        Foreground="#E8EDF4">
    <Window.InputBindings>
        <KeyBinding Key="N" Modifiers="Control" Command="{Binding NewProjectCommand}" />
        <KeyBinding Key="O" Modifiers="Control" Command="{Binding OpenProjectCommand}" />
        <KeyBinding Key="S" Modifiers="Control" Command="{Binding SaveProjectCommand}" />
        <KeyBinding Key="S" Modifiers="Control+Shift" Command="{Binding SaveProjectAsCommand}" />
        <KeyBinding Key="I" Modifiers="Control" Command="{Binding OpenSourcesCommand}" />
        <KeyBinding Key="Enter" Modifiers="Control" Command="{Binding AddCurrentCropToFigureCommand}" />
        <KeyBinding Key="Z" Modifiers="Control" Command="{Binding UndoCommand}" />
        <KeyBinding Key="Y" Modifiers="Control" Command="{Binding RedoCommand}" />
        <KeyBinding Key="Z" Modifiers="Control+Shift" Command="{Binding RedoCommand}" />
    </Window.InputBindings>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibility" />

        <SolidColorBrush x:Key="PanelBrush" Color="#141A22" />
        <SolidColorBrush x:Key="PanelRaisedBrush" Color="#1A222D" />
        <SolidColorBrush x:Key="BorderBrush" Color="#2B3543" />
        <SolidColorBrush x:Key="MutedBrush" Color="#8F9BAD" />
        <SolidColorBrush x:Key="AccentBrush" Color="#22C7E8" />

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="#E8EDF4" />
            <Setter Property="Background" Value="#202A36" />
            <Setter Property="BorderBrush" Value="#344253" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,7" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#293646" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#16222C" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.38" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#087E98" />
            <Setter Property="BorderBrush" Value="#26D8F6" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="ToolButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0,0,0,2" />
            <Style.Triggers>
                <Trigger Property="Tag" Value="True">
                    <Setter Property="Background" Value="#087E98" />
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                    <Setter Property="Foreground" Value="#F5FBFF" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="HeaderButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="FontSize" Value="11" />
        </Style>

        <Style x:Key="CompactToolButtonStyle" TargetType="Button" BasedOn="{StaticResource ToolButtonStyle}">
            <Setter Property="Padding" Value="7,5" />
            <Setter Property="FontSize" Value="11" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="#101820" />
            <Setter Property="Background" Value="#0F151D" />
            <Setter Property="BorderBrush" Value="#354253" />
            <Setter Property="Padding" Value="7,5" />
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="#0F151D" />
            <Setter Property="Foreground" Value="#E8EDF4" />
            <Setter Property="BorderBrush" Value="#26313D" />
            <Setter Property="Padding" Value="7,3" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#174B59" />
                    <Setter Property="Foreground" Value="#FFFFFF" />
                    <Setter Property="BorderBrush" Value="#22C7E8" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1A222D" />
            <Setter Property="Foreground" Value="#AEB9C7" />
            <Setter Property="BorderBrush" Value="#2B3543" />
            <Setter Property="Padding" Value="7,5" />
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#0F151D" />
            <Setter Property="Foreground" Value="#E8EDF4" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#174B59" />
                    <Setter Property="Foreground" Value="#FFFFFF" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="#F4F7FA" />
            <Setter Property="Background" Value="#0F151D" />
            <Setter Property="BorderBrush" Value="#354253" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="CaretBrush" Value="#22C7E8" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#DDE5EE" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#AEB9C7" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="98" />
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.RowSpan="2" Background="#121820" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                    <RowDefinition Height="98" />
                </Grid.RowDefinitions>
                <Menu Grid.Row="0" Margin="8,0" Background="Transparent" Foreground="#D8E0E8" VerticalContentAlignment="Center">
                    <Menu.Resources>
                        <Style TargetType="MenuItem">
                            <Setter Property="Foreground" Value="#D8E0E8" />
                            <Setter Property="Background" Value="#171E27" />
                            <Setter Property="Padding" Value="10,5" />
                        </Style>
                    </Menu.Resources>
                    <MenuItem Header="文件(F)">
                        <MenuItem Header="新建工程" Command="{Binding NewProjectCommand}" />
                        <MenuItem Header="打开工程…" Command="{Binding OpenProjectCommand}" />
                        <MenuItem Header="保存工程" Command="{Binding SaveProjectCommand}" />
                        <MenuItem Header="另存为…" Command="{Binding SaveProjectAsCommand}" />
                        <Separator />
                        <MenuItem Header="导入图像…" Command="{Binding OpenSourcesCommand}" />
                    </MenuItem>
                    <MenuItem Header="编辑(E)">
                        <MenuItem Header="撤销" Command="{Binding UndoCommand}" />
                        <MenuItem Header="重做" Command="{Binding RedoCommand}" />
                    </MenuItem>
                    <MenuItem Header="视图(V)">
                        <MenuItem Header="裁剪视图" Command="{Binding ShowCropWorkspaceCommand}" />
                        <MenuItem Header="拼版视图" Command="{Binding ShowFigureWorkspaceCommand}" />
                        <Separator />
                        <MenuItem Header="检查器" Command="{Binding ShowInspectorTabCommand}" />
                        <MenuItem Header="图层" Command="{Binding ShowLayersTabCommand}" />
                    </MenuItem>
                    <MenuItem Header="图像(I)">
                        <MenuItem Header="导出当前裁剪" Command="{Binding ExportCropCommand}" />
                        <MenuItem Header="批量导出裁剪" Command="{Binding ExportBatchCropsCommand}" />
                    </MenuItem>
                    <MenuItem Header="测量(A)">
                        <MenuItem Header="标定参考线" Command="{Binding SelectCalibrationToolCommand}" />
                        <Separator />
                        <MenuItem Header="长度" Command="{Binding SelectLengthToolCommand}" />
                        <MenuItem Header="角度（三点）" Command="{Binding SelectAngleToolCommand}" />
                        <MenuItem Header="矩形 ROI" Command="{Binding SelectRectangleRoiToolCommand}" />
                        <MenuItem Header="圆形测量" Command="{Binding SelectCircleRoiToolCommand}" />
                        <MenuItem Header="折线（双击完成）" Command="{Binding SelectPolylineToolCommand}" />
                        <Separator />
                        <MenuItem Header="复制测量表" Command="{Binding CopyMeasurementsCommand}" />
                        <MenuItem Header="导出测量 CSV / Excel" Command="{Binding ExportMeasurementsCommand}" />
                    </MenuItem>
                    <MenuItem Header="裁剪(C)">
                        <MenuItem Header="加入当前裁剪到拼版" Command="{Binding AddCurrentCropToFigureCommand}" />
                        <MenuItem Header="加入批量裁剪队列" Command="{Binding AddCurrentCropToBatchQueueCommand}" />
                    </MenuItem>
                    <MenuItem Header="拼版(M)">
                        <MenuItem Header="导出整张拼版" Command="{Binding ExportFigureCommand}" />
                        <MenuItem Header="批量导出投稿版本" Command="{Binding ExportFigureVariantsCommand}" />
                        <Separator />
                        <MenuItem Header="运行 Figure QC" Command="{Binding RunFigureQcCommand}" />
                    </MenuItem>
                    <MenuItem Header="工具(T)">
                        <MenuItem Header="{Binding TemplateLibraryLabel}" Command="{Binding ShowFigureWorkspaceCommand}" />
                        <MenuItem Header="导入用户模板" Command="{Binding ImportTemplateCommand}" />
                    </MenuItem>
                    <MenuItem Header="帮助(H)">
                        <MenuItem Header="关于与快捷键" Command="{Binding ShowHelpCommand}" />
                    </MenuItem>
                </Menu>
                <Grid Grid.Row="1" Margin="14,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50" />
                        <RowDefinition Height="48" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Width="28" Height="28" Margin="0,0,8,0" Background="#0A829B" CornerRadius="6">
                                <TextBlock Text="SC" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="10" />
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="SciCanvas" FontSize="15" FontWeight="SemiBold" />
                                <TextBlock Text="LOCAL · NON-DESTRUCTIVE" Foreground="{StaticResource MutedBrush}" FontSize="8" />
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="18,0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Button Command="{Binding OpenSourcesCommand}" Style="{StaticResource AccentButtonStyle}" Padding="9,5" FontSize="11" Content="导入图像" />
                            <Button Margin="6,0,0,0" Command="{Binding NewProjectCommand}" Style="{StaticResource HeaderButtonStyle}" Content="新建" />
                            <Button Margin="6,0,0,0" Command="{Binding OpenProjectCommand}" Style="{StaticResource HeaderButtonStyle}" Content="打开" />
                            <Button Margin="6,0,0,0" Command="{Binding SaveProjectCommand}" Style="{StaticResource HeaderButtonStyle}" Content="保存" />
                            <Button Margin="6,0,0,0" Command="{Binding UndoCommand}" Style="{StaticResource HeaderButtonStyle}" Content="撤销" ToolTip="Ctrl+Z" />
                            <Button Margin="6,0,0,0" Command="{Binding RedoCommand}" Style="{StaticResource HeaderButtonStyle}" Content="重做" ToolTip="Ctrl+Y 或 Ctrl+Shift+Z" />
                        </StackPanel>
                        <Border Grid.Column="2" Padding="8,4" Background="#10251E" BorderBrush="#256B51" BorderThickness="1" CornerRadius="12" VerticalAlignment="Center">
                            <TextBlock Text="源文件只读保护" Foreground="#75D9AA" FontSize="10" />
                        </Border>
                    </Grid>
                    <Border Grid.Row="1" Margin="0,3,0,5" Padding="6,0" Background="#0F151D" BorderBrush="#26313D" BorderThickness="1" CornerRadius="5">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Button Command="{Binding SelectCropToolCommand}" Style="{StaticResource CompactToolButtonStyle}" Tag="{Binding IsCropToolActive}" Content="裁剪" ToolTip="V · 裁剪工具" />
                            <Button Command="{Binding SelectCalibrationToolCommand}" Style="{StaticResource CompactToolButtonStyle}" Tag="{Binding IsCalibrationToolActive}" Content="标定" ToolTip="K · 拖动参考线建立空间尺度" />
                            <Button Command="{Binding SelectLengthToolCommand}" Style="{StaticResource CompactToolButtonStyle}" Tag="{Binding IsLengthToolActive}" Content="长度" ToolTip="L" />
                            <Button Command="{Binding SelectAngleToolCommand}" Style="{StaticResource CompactToolButtonStyle}" Tag="{Binding IsAngleToolActive}" Content="角度" ToolTip="A" />
                            <Button Command="{Binding SelectRectangleRoiToolCommand}" Style="{StaticResource CompactToolButtonStyle}" Tag="{Binding IsRectangleRoiToolActive}" Content="矩形 ROI" ToolTip="R" />
                            <Button Command="{Binding SelectCircleRoiToolCommand}" Style="{StaticResource CompactToolButtonStyle}" Tag="{Binding IsCircleRoiToolActive}" Content="圆形" ToolTip="E" />
                            <Button Command="{Binding SelectPolylineToolCommand}" Style="{StaticResource CompactToolButtonStyle}" Tag="{Binding IsPolylineToolActive}" Content="折线" ToolTip="P" />
                            <Button Margin="12,0,0,0" Command="{Binding ShowFigureWorkspaceCommand}" Style="{StaticResource CompactToolButtonStyle}" Content="拼版视图" />
                            <Button Margin="6,0,0,0" Command="{Binding AddCurrentCropToFigureCommand}" Style="{StaticResource AccentButtonStyle}" Padding="9,5" FontSize="11" Content="加入拼版" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.75*" MinWidth="160" MaxWidth="340" />
                <ColumnDefinition Width="6" />
                <ColumnDefinition Width="2*" MinWidth="280" />
                <ColumnDefinition Width="6" />
                <ColumnDefinition Width="1.05*" MinWidth="230" MaxWidth="420" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource PanelBrush}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="54" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="14,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="源图像" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" Text="{Binding Sources.Count, StringFormat={}{0} 项}" Foreground="{StaticResource MutedBrush}" FontSize="11" VerticalAlignment="Center" />
                    </Grid>

                    <ListBox Grid.Row="1"
                             Margin="8,0,8,8"
                             Padding="0"
                             Background="Transparent"
                             BorderThickness="0"
                             ItemsSource="{Binding Sources}"
                             SelectedItem="{Binding SelectedSource}"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="0,0,0,6" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#203442" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="8" Background="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=Background}" CornerRadius="5">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="66" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Border Width="58" Height="58" Background="#0B0F14" CornerRadius="3" ClipToBounds="True">
                                            <Image Source="{Binding Preview}" Stretch="Uniform" />
                                        </Border>
                                        <StackPanel Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding DisplayName}" FontSize="12" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" ToolTip="{Binding OriginalPath}" />
                                            <TextBlock Margin="0,4,0,0" Text="{Binding DimensionsText}" Foreground="#B8C4D1" FontSize="10" />
                                            <TextBlock Margin="0,2,0,0"
                                                       Text="{Binding DetailsText, Mode=OneWay}"
                                                       Foreground="{StaticResource MutedBrush}"
                                                       FontSize="9" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Border Grid.Row="2" Margin="12" Padding="10" Background="#101820" BorderBrush="#284455" BorderThickness="1" CornerRadius="4">
                        <StackPanel>
                            <TextBlock Text="只读导入" Foreground="#7EDDF0" FontSize="11" FontWeight="SemiBold" />
                            <TextBlock Margin="0,4,0,0" Text="原图路径、SHA-256 与 Windows 文件标识会被记录；导出不会覆盖源文件。" Foreground="{StaticResource MutedBrush}" FontSize="10" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#263442" ResizeDirection="Columns" ResizeBehavior="PreviousAndNext" ShowsPreview="True" ToolTip="拖动调整源图像栏宽度" />

            <Grid Grid.Column="2" Background="#090D12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="42" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Visibility="{Binding CropWorkspaceVisibility}" Background="#11171F" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                    <Grid Margin="14,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="源图画布 · 像素坐标" Foreground="#B8C3CF" VerticalAlignment="Center" FontSize="11" FontWeight="SemiBold" />
                        <TextBlock Grid.Column="1" Margin="16,0" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{StaticResource MutedBrush}" FontSize="10" TextTrimming="CharacterEllipsis"
                                   Text="{Binding ActiveScienceToolHint}" />
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Width="30" Height="28" Padding="0" Click="SourceZoomOut_OnClick" Content="−" ToolTip="缩小 · Ctrl+-" />
                            <TextBlock x:Name="SourceZoomText" Width="54" Text="适合" TextAlignment="Center" VerticalAlignment="Center" Foreground="#8CE5F4" FontSize="10" />
                            <Button Width="30" Height="28" Padding="0" Click="SourceZoomIn_OnClick" Content="＋" ToolTip="放大 · Ctrl++" />
                            <Button Margin="6,0,0,0" Padding="8,4" Click="SourceZoomFit_OnClick" Content="适合" ToolTip="适合窗口 · Ctrl+0" />
                            <Button Margin="4,0,0,0" Padding="8,4" Click="SourceZoomActual_OnClick" Content="1:1" ToolTip="100% · Ctrl+1" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid Grid.Row="1" Margin="24" ClipToBounds="True" Visibility="{Binding CropWorkspaceVisibility}">
                    <Border Background="#0C1219" BorderBrush="#202B36" BorderThickness="1" />

                    <StackPanel Visibility="{Binding EmptyStateVisibility}" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border Width="84" Height="84" Background="#121D27" BorderBrush="#294250" BorderThickness="1" CornerRadius="42">
                            <TextBlock Text="＋" Foreground="#4BD4EF" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <TextBlock Margin="0,18,0,0" Text="导入科研图像开始裁剪" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" />
                        <TextBlock Margin="0,8,0,0" Text="支持 TIFF、PNG、JPEG、BMP · 全程本地处理" Foreground="{StaticResource MutedBrush}" FontSize="11" />
                        <Button Margin="0,20,0,0" Command="{Binding OpenSourcesCommand}" Style="{StaticResource AccentButtonStyle}" Content="选择图像" HorizontalAlignment="Center" />
                    </StackPanel>

                    <ScrollViewer x:Name="ImageViewport"
                                  Margin="12"
                                  HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalContentAlignment="Center"
                                  VerticalContentAlignment="Center"
                                  CanContentScroll="False"
                                  PreviewMouseWheel="ImageViewport_OnPreviewMouseWheel"
                                  PreviewMouseDown="Viewport_OnPreviewMouseDown"
                                  PreviewMouseMove="Viewport_OnPreviewMouseMove"
                                  PreviewMouseUp="Viewport_OnPreviewMouseUp"
                                  SizeChanged="ImageViewport_OnSizeChanged">
                        <Canvas x:Name="ImageCanvas"
                                Width="{Binding SelectedSource.Width, FallbackValue=1}"
                                Height="{Binding SelectedSource.Height, FallbackValue=1}"
                                Background="Transparent"
                                Cursor="Cross"
                                MouseLeftButtonDown="ImageCanvas_OnMouseLeftButtonDown"
                                MouseMove="ImageCanvas_OnMouseMove"
                                MouseLeftButtonUp="ImageCanvas_OnMouseLeftButtonUp"
                                LostMouseCapture="ImageCanvas_OnLostMouseCapture"
                                SizeChanged="ImageCanvas_OnSizeChanged">
                            <Canvas.LayoutTransform>
                                <ScaleTransform x:Name="ImageZoomTransform" ScaleX="1" ScaleY="1" />
                            </Canvas.LayoutTransform>
                            <Image Width="{Binding SelectedSource.Width}"
                                   Height="{Binding SelectedSource.Height}"
                                   Source="{Binding SelectedSource.Preview}"
                                   Stretch="Fill"
                                   RenderOptions.BitmapScalingMode="HighQuality" />
                            <Grid x:Name="CropOverlay"
                                  Canvas.Left="{Binding Crop.X}"
                                  Canvas.Top="{Binding Crop.Y}"
                                  Panel.ZIndex="2"
                                  Width="{Binding Crop.Width}"
                                  Height="{Binding Crop.Height}"
                                  Cursor="SizeAll"
                                  Visibility="{Binding CropOverlayVisibility}">
                                <Rectangle Fill="#2400D7FF"
                                           Stroke="#17D9F4"
                                           StrokeThickness="5" />
                                <Border HorizontalAlignment="Left" VerticalAlignment="Top" Margin="7" Padding="7,3" Background="#D011171F" BorderBrush="#17D9F4" BorderThickness="1" CornerRadius="3" IsHitTestVisible="False">
                                    <TextBlock Text="{Binding Crop.SizeText}" Foreground="#8CE5F4" FontSize="18" FontWeight="SemiBold" />
                                </Border>
                                <Thumb x:Name="CropTopLeftHandle" Uid="TopLeft" Width="26" Height="26" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-13" Background="#FFF5FBFF" BorderBrush="#FF087E98" BorderThickness="5" Cursor="SizeNWSE" PreviewMouseLeftButtonDown="CropResizeHandle_OnMouseLeftButtonDown" PreviewMouseLeftButtonUp="CropResizeHandle_OnMouseLeftButtonUp" LostMouseCapture="CropResizeHandle_OnLostMouseCapture" />
                                <Thumb x:Name="CropTopHandle" Uid="Top" Width="30" Height="18" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,-9,0,0" Background="#FFF5FBFF" BorderBrush="#FF087E98" BorderThickness="5" Cursor="SizeNS" PreviewMouseLeftButtonDown="CropResizeHandle_OnMouseLeftButtonDown" PreviewMouseLeftButtonUp="CropResizeHandle_OnMouseLeftButtonUp" LostMouseCapture="CropResizeHandle_OnLostMouseCapture" />
                                <Thumb x:Name="CropTopRightHandle" Uid="TopRight" Width="26" Height="26" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="-13" Background="#FFF5FBFF" BorderBrush="#FF087E98" BorderThickness="5" Cursor="SizeNESW" PreviewMouseLeftButtonDown="CropResizeHandle_OnMouseLeftButtonDown" PreviewMouseLeftButtonUp="CropResizeHandle_OnMouseLeftButtonUp" LostMouseCapture="CropResizeHandle_OnLostMouseCapture" />
                                <Thumb x:Name="CropRightHandle" Uid="Right" Width="18" Height="30" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,-9,0" Background="#FFF5FBFF" BorderBrush="#FF087E98" BorderThickness="5" Cursor="SizeWE" PreviewMouseLeftButtonDown="CropResizeHandle_OnMouseLeftButtonDown" PreviewMouseLeftButtonUp="CropResizeHandle_OnMouseLeftButtonUp" LostMouseCapture="CropResizeHandle_OnLostMouseCapture" />
                                <Thumb x:Name="CropBottomRightHandle" Uid="BottomRight" Width="26" Height="26" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="-13" Background="#FFF5FBFF" BorderBrush="#FF087E98" BorderThickness="5" Cursor="SizeNWSE" PreviewMouseLeftButtonDown="CropResizeHandle_OnMouseLeftButtonDown" PreviewMouseLeftButtonUp="CropResizeHandle_OnMouseLeftButtonUp" LostMouseCapture="CropResizeHandle_OnLostMouseCapture" />
                                <Thumb x:Name="CropBottomHandle" Uid="Bottom" Width="30" Height="18" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,-9" Background="#FFF5FBFF" BorderBrush="#FF087E98" BorderThickness="5" Cursor="SizeNS" PreviewMouseLeftButtonDown="CropResizeHandle_OnMouseLeftButtonDown" PreviewMouseLeftButtonUp="CropResizeHandle_OnMouseLeftButtonUp" LostMouseCapture="CropResizeHandle_OnLostMouseCapture" />
                                <Thumb x:Name="CropBottomLeftHandle" Uid="BottomLeft" Width="26" Height="26" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="-13" Background="#FFF5FBFF" BorderBrush="#FF087E98" BorderThickness="5" Cursor="SizeNESW" PreviewMouseLeftButtonDown="CropResizeHandle_OnMouseLeftButtonDown" PreviewMouseLeftButtonUp="CropResizeHandle_OnMouseLeftButtonUp" LostMouseCapture="CropResizeHandle_OnLostMouseCapture" />
                                <Thumb x:Name="CropLeftHandle" Uid="Left" Width="18" Height="30" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="-9,0,0,0" Background="#FFF5FBFF" BorderBrush="#FF087E98" BorderThickness="5" Cursor="SizeWE" PreviewMouseLeftButtonDown="CropResizeHandle_OnMouseLeftButtonDown" PreviewMouseLeftButtonUp="CropResizeHandle_OnMouseLeftButtonUp" LostMouseCapture="CropResizeHandle_OnLostMouseCapture" />
                            </Grid>
                            <Line X1="{Binding SelectedSource.Calibration.ReferenceStartX}"
                                  Y1="{Binding SelectedSource.Calibration.ReferenceStartY}"
                                  X2="{Binding SelectedSource.Calibration.ReferenceEndX}"
                                  Y2="{Binding SelectedSource.Calibration.ReferenceEndY}"
                                  Stroke="#FF22C7E8"
                                  StrokeThickness="5"
                                  Panel.ZIndex="10"
                                  IsHitTestVisible="False"
                                  Visibility="{Binding SelectedSource.Calibration.ReferenceLineVisibility}" />
                            <Border Canvas.Left="{Binding SelectedSource.Calibration.ReferenceLabelX}"
                                    Canvas.Top="{Binding SelectedSource.Calibration.ReferenceLabelY}"
                                    Panel.ZIndex="11"
                                    Padding="10,5"
                                    Background="#D011171F"
                                    BorderBrush="#FF22C7E8"
                                    BorderThickness="1"
                                    CornerRadius="3"
                                    IsHitTestVisible="False"
                                    Visibility="{Binding SelectedSource.Calibration.ReferenceLineVisibility}">
                                <TextBlock Text="{Binding SelectedSource.Calibration.ReferenceLabel}"
                                           Foreground="#FF4BD4EF"
                                           FontSize="22"
                                           FontWeight="SemiBold" />
                            </Border>
                            <ItemsControl Width="{Binding SelectedSource.Width}"
                                          Height="{Binding SelectedSource.Height}"
                                          ItemsSource="{Binding SelectedSource.Measurements}"
                                          Panel.ZIndex="20">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <Canvas />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemContainerStyle>
                                    <Style TargetType="ContentPresenter">
                                        <Setter Property="Canvas.Left" Value="0" />
                                        <Setter Property="Canvas.Top" Value="0" />
                                    </Style>
                                </ItemsControl.ItemContainerStyle>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Canvas Width="{Binding DataContext.SelectedSource.Width, RelativeSource={RelativeSource AncestorType=Window}}"
                                                Height="{Binding DataContext.SelectedSource.Height, RelativeSource={RelativeSource AncestorType=Window}}"
                                                Tag="{Binding}"
                                                Cursor="Hand"
                                                MouseLeftButtonDown="Measurement_OnMouseLeftButtonDown"
                                                PreviewMouseMove="Measurement_OnMouseMove"
                                                PreviewMouseLeftButtonUp="Measurement_OnMouseLeftButtonUp"
                                                LostMouseCapture="Measurement_OnLostMouseCapture">
                                            <Line X1="{Binding X1}" Y1="{Binding Y1}" X2="{Binding X2}" Y2="{Binding Y2}"
                                                  Stroke="Transparent" StrokeThickness="24"
                                                  Visibility="{Binding LengthVisibility}" />
                                            <Line X1="{Binding X1}" Y1="{Binding Y1}" X2="{Binding X2}" Y2="{Binding Y2}"
                                                  Stroke="{Binding OverlayStroke}" StrokeThickness="{Binding OverlayThickness}"
                                                  StrokeDashArray="{Binding StrokeDashArray}" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                                  IsHitTestVisible="False"
                                                  Visibility="{Binding LengthVisibility}" />
                                            <Polyline Points="{Binding PolylinePoints}"
                                                      Stroke="Transparent"
                                                      StrokeThickness="24"
                                                      StrokeLineJoin="Round"
                                                      Visibility="{Binding PolylineVisibility}" />
                                            <Polyline Points="{Binding PolylinePoints}"
                                                      Stroke="{Binding OverlayStroke}"
                                                      StrokeThickness="{Binding OverlayThickness}"
                                                      StrokeDashArray="{Binding StrokeDashArray}"
                                                      StrokeLineJoin="Round"
                                                      StrokeStartLineCap="Round"
                                                      StrokeEndLineCap="Round"
                                                      IsHitTestVisible="False"
                                                      Visibility="{Binding PolylineVisibility}" />
                                            <Line X1="{Binding X1}" Y1="{Binding Y1}" X2="{Binding X2}" Y2="{Binding Y2}"
                                                  Stroke="Transparent" StrokeThickness="24"
                                                  Visibility="{Binding AngleVisibility}" />
                                            <Line X1="{Binding X2}" Y1="{Binding Y2}" X2="{Binding X3}" Y2="{Binding Y3}"
                                                  Stroke="Transparent" StrokeThickness="24"
                                                  Visibility="{Binding AngleVisibility}" />
                                            <Line X1="{Binding X1}" Y1="{Binding Y1}" X2="{Binding X2}" Y2="{Binding Y2}"
                                                  Stroke="{Binding OverlayStroke}" StrokeThickness="{Binding OverlayThickness}"
                                                  StrokeDashArray="{Binding StrokeDashArray}" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                                  IsHitTestVisible="False"
                                                  Visibility="{Binding AngleVisibility}" />
                                            <Line X1="{Binding X2}" Y1="{Binding Y2}" X2="{Binding X3}" Y2="{Binding Y3}"
                                                  Stroke="{Binding OverlayStroke}" StrokeThickness="{Binding OverlayThickness}"
                                                  StrokeDashArray="{Binding StrokeDashArray}" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                                  IsHitTestVisible="False"
                                                  Visibility="{Binding AngleVisibility}" />
                                            <Rectangle Canvas.Left="{Binding RectangleX}"
                                                       Canvas.Top="{Binding RectangleY}"
                                                       Width="{Binding RectangleWidth}"
                                                       Height="{Binding RectangleHeight}"
                                                       Stroke="{Binding OverlayStroke}"
                                                       StrokeThickness="{Binding OverlayThickness}"
                                                       StrokeDashArray="{Binding StrokeDashArray}"
                                                       Fill="{Binding OverlayFill}"
                                                       Visibility="{Binding RectangleVisibility}" />
                                            <Ellipse Canvas.Left="{Binding RectangleX}"
                                                     Canvas.Top="{Binding RectangleY}"
                                                     Width="{Binding RectangleWidth}"
                                                     Height="{Binding RectangleHeight}"
                                                     Stroke="{Binding OverlayStroke}"
                                                     StrokeThickness="{Binding OverlayThickness}"
                                                     StrokeDashArray="{Binding StrokeDashArray}"
                                                     Fill="{Binding OverlayFill}"
                                                     Visibility="{Binding CircleVisibility}" />
                                            <Ellipse Canvas.Left="{Binding PointAMarkerX}" Canvas.Top="{Binding PointAMarkerY}"
                                                     Width="{Binding MarkerSizePixels}" Height="{Binding MarkerSizePixels}" Fill="#FF11171F"
                                                     Stroke="{Binding OverlayStroke}" StrokeThickness="3" IsHitTestVisible="False"
                                                     Visibility="{Binding MarkerVisibility}" />
                                            <Ellipse Canvas.Left="{Binding PointBMarkerX}" Canvas.Top="{Binding PointBMarkerY}"
                                                     Width="{Binding MarkerSizePixels}" Height="{Binding MarkerSizePixels}" Fill="#FF11171F"
                                                     Stroke="{Binding OverlayStroke}" StrokeThickness="3" IsHitTestVisible="False"
                                                     Visibility="{Binding MarkerVisibility}" />
                                            <Ellipse Canvas.Left="{Binding PointCMarkerX}" Canvas.Top="{Binding PointCMarkerY}"
                                                     Width="{Binding MarkerSizePixels}" Height="{Binding MarkerSizePixels}" Fill="#FF11171F"
                                                     Stroke="{Binding OverlayStroke}" StrokeThickness="3" IsHitTestVisible="False"
                                                     Visibility="{Binding PointCMarkerVisibility}" />
                                            <Border Canvas.Left="{Binding LabelX}"
                                                    Canvas.Top="{Binding LabelY}"
                                                    Padding="9,4"
                                                    Background="#D011171F"
                                                    BorderBrush="{Binding OverlayStroke}"
                                                    BorderThickness="1"
                                                    CornerRadius="3"
                                                    IsHitTestVisible="False"
                                                    Visibility="{Binding LabelVisibility}">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Number, StringFormat={}{0} · }" Foreground="#FFB8C3CF" FontSize="20" />
                                                    <TextBlock Text="{Binding ValueText}" Foreground="{Binding OverlayStroke}" FontSize="22" FontWeight="SemiBold" />
                                                </StackPanel>
                                            </Border>
                                            <Thumb x:Name="MeasurementPointAHandle"
                                                   Canvas.Left="{Binding PointAHandleX}"
                                                   Canvas.Top="{Binding PointAHandleY}"
                                                   Width="{Binding SelectionHandleSizePixels}"
                                                   Height="{Binding SelectionHandleSizePixels}"
                                                   Background="#FFF5FBFF"
                                                   BorderBrush="#FFFFB300"
                                                   BorderThickness="6"
                                                   Cursor="SizeAll"
                                                   Tag="{Binding}"
                                                   Visibility="{Binding SelectionHandleVisibility}"
                                                   PreviewMouseLeftButtonDown="MeasurementResizeHandle_OnMouseLeftButtonDown" />
                                            <Thumb x:Name="MeasurementPointBHandle"
                                                   Canvas.Left="{Binding PointBHandleX}"
                                                   Canvas.Top="{Binding PointBHandleY}"
                                                   Width="{Binding SelectionHandleSizePixels}"
                                                   Height="{Binding SelectionHandleSizePixels}"
                                                   Background="#FFF5FBFF"
                                                   BorderBrush="#FFFFB300"
                                                   BorderThickness="6"
                                                   Cursor="SizeAll"
                                                   Tag="{Binding}"
                                                   Visibility="{Binding SelectionHandleVisibility}"
                                                   PreviewMouseLeftButtonDown="MeasurementResizeHandle_OnMouseLeftButtonDown" />
                                            <Thumb x:Name="MeasurementPointCHandle"
                                                   Canvas.Left="{Binding PointCHandleX}"
                                                   Canvas.Top="{Binding PointCHandleY}"
                                                   Width="{Binding SelectionHandleSizePixels}"
                                                   Height="{Binding SelectionHandleSizePixels}"
                                                   Background="#FFF5FBFF"
                                                   BorderBrush="#FFFFB300"
                                                   BorderThickness="6"
                                                   Cursor="SizeAll"
                                                   Tag="{Binding}"
                                                   Visibility="{Binding PointCHandleVisibility}"
                                                   PreviewMouseLeftButtonDown="MeasurementResizeHandle_OnMouseLeftButtonDown" />
                                        </Canvas>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <ItemsControl Width="{Binding SelectedSource.Width}"
                                          Height="{Binding SelectedSource.Height}"
                                          ItemsSource="{Binding AssistedRegions}"
                                          Panel.ZIndex="18"
                                          IsHitTestVisible="False">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <Canvas />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemContainerStyle>
                                    <Style TargetType="ContentPresenter">
                                        <Setter Property="Canvas.Left" Value="{Binding X}" />
                                        <Setter Property="Canvas.Top" Value="{Binding Y}" />
                                    </Style>
                                </ItemsControl.ItemContainerStyle>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Rectangle Width="{Binding Width}"
                                                       Height="{Binding Height}"
                                                       Stroke="{Binding OverlayStroke}"
                                                       StrokeThickness="3"
                                                       Fill="{Binding OverlayFill}" />
                                            <Border Margin="2"
                                                    Padding="3,1"
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Top"
                                                    Background="#CC11171F"
                                                    BorderBrush="{Binding OverlayStroke}"
                                                    BorderThickness="1">
                                                <TextBlock Text="{Binding Id}"
                                                           Foreground="{Binding OverlayStroke}"
                                                           FontSize="12"
                                                           FontWeight="Bold" />
                                            </Border>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Canvas>
                    </ScrollViewer>
                </Grid>

                <Border Grid.Row="0" Visibility="{Binding FigureWorkspaceVisibility}" Background="#11171F" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                    <Grid Margin="14,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="论文拼版 · 模板像素坐标" Foreground="#B8C3CF" VerticalAlignment="Center" FontSize="11" FontWeight="SemiBold" />
                        <TextBlock Grid.Column="1" Margin="16,0" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{StaticResource MutedBrush}" FontSize="10" TextTrimming="CharacterEllipsis"
                                   Text="Ctrl+单击多选 · 空格拖动画布 · Delete 删除选中" />
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Width="30" Height="28" Padding="0" Click="FigureZoomOut_OnClick" Content="−" ToolTip="缩小 · Ctrl+-" />
                            <TextBlock x:Name="FigureZoomText" Width="54" Text="适合" TextAlignment="Center" VerticalAlignment="Center" Foreground="#8CE5F4" FontSize="10" />
                            <Button Width="30" Height="28" Padding="0" Click="FigureZoomIn_OnClick" Content="＋" ToolTip="放大 · Ctrl++" />
                            <Button Margin="6,0,0,0" Padding="8,4" Click="FigureZoomFit_OnClick" Content="适合" ToolTip="适合窗口 · Ctrl+0" />
                            <Button Margin="4,0,0,0" Padding="8,4" Click="FigureZoomActual_OnClick" Content="1:1" ToolTip="100% · Ctrl+1" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid Grid.Row="1" Margin="24" ClipToBounds="True" Visibility="{Binding FigureWorkspaceVisibility}">
                    <Border Background="#0C1219" BorderBrush="#202B36" BorderThickness="1" />
                    <ScrollViewer x:Name="FigureViewport"
                                  Margin="12"
                                  HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalContentAlignment="Center"
                                  VerticalContentAlignment="Center"
                                  CanContentScroll="False"
                                  PreviewMouseWheel="FigureViewport_OnPreviewMouseWheel"
                                  PreviewMouseDown="Viewport_OnPreviewMouseDown"
                                  PreviewMouseMove="Viewport_OnPreviewMouseMove"
                                  PreviewMouseUp="Viewport_OnPreviewMouseUp"
                                  SizeChanged="FigureViewport_OnSizeChanged">
                        <Grid x:Name="FigureSurface"
                              Width="{Binding Figure.CanvasWidth}"
                              Height="{Binding Figure.CanvasHeight}"
                              SizeChanged="FigureSurface_OnSizeChanged"
                              Background="{Binding Figure.BackgroundBrush}">
                            <Grid.LayoutTransform>
                                <ScaleTransform x:Name="FigureZoomTransform" ScaleX="1" ScaleY="1" />
                            </Grid.LayoutTransform>
                            <ListBox Width="{Binding Figure.CanvasWidth}"
                                     Height="{Binding Figure.CanvasHeight}"
                                     Padding="0"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     ItemsSource="{Binding Figure.Panels}"
                                     SelectionMode="Extended">
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <Canvas />
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Canvas.Left" Value="{Binding X}" />
                                        <Setter Property="Canvas.Top" Value="{Binding Y}" />
                                        <Setter Property="Panel.ZIndex" Value="{Binding ZIndex}" />
                                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                                        <Setter Property="Width" Value="{Binding Width}" />
                                        <Setter Property="Height" Value="{Binding Height}" />
                                        <Setter Property="Padding" Value="0" />
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                        <Setter Property="VerticalContentAlignment" Value="Stretch" />
                                        <Setter Property="Background" Value="Transparent" />
                                        <Setter Property="BorderThickness" Value="0" />
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Tag="{Binding}"
                                                Background="#F2F4F6"
                                                Cursor="SizeAll"
                                                ClipToBounds="True"
                                                PreviewMouseLeftButtonDown="FigurePanel_OnMouseLeftButtonDown"
                                                PreviewMouseMove="FigurePanel_OnMouseMove"
                                                PreviewMouseLeftButtonUp="FigurePanel_OnMouseLeftButtonUp"
                                                LostMouseCapture="FigurePanel_OnLostMouseCapture"
                                                Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibility}}">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="BorderBrush" Value="#4C5968" />
                                                    <Setter Property="BorderThickness" Value="3" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                            <Setter Property="BorderBrush" Value="#16C7E8" />
                                                            <Setter Property="BorderThickness" Value="7" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <Grid>
                                                <Image Source="{Binding Preview}" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality" />
                                                <Canvas IsHitTestVisible="False"
                                                        Visibility="{Binding HasRenderableScaleBar, Converter={StaticResource BooleanToVisibility}}">
                                                    <TextBlock Canvas.Left="{Binding ScaleBarPreviewX}"
                                                               Canvas.Top="{Binding ScaleBarLabelPreviewY}"
                                                               Padding="5,2"
                                                               Text="{Binding ScaleBarLabel}"
                                                               Foreground="{Binding DataContext.Figure.GlobalScaleBarBrush, RelativeSource={RelativeSource AncestorType=Window}}"
                                                               Background="#B0000000"
                                                               FontFamily="{Binding DataContext.Figure.GlobalFontFamily, RelativeSource={RelativeSource AncestorType=Window}}"
                                                               FontSize="{Binding DataContext.Figure.GlobalFontSizePixels, RelativeSource={RelativeSource AncestorType=Window}}"
                                                               FontWeight="Bold" />
                                                    <Border Canvas.Left="{Binding ScaleBarPreviewX}"
                                                            Canvas.Top="{Binding ScaleBarPreviewY}"
                                                            Width="{Binding ScaleBarPreviewWidth}"
                                                            Height="{Binding DataContext.Figure.GlobalStrokeWidthPixels, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            Background="{Binding DataContext.Figure.GlobalScaleBarBrush, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            BorderBrush="Black"
                                                            BorderThickness="2" />
                                                </Canvas>
                                                <Border HorizontalAlignment="Left"
                                                        VerticalAlignment="Top"
                                                        Margin="8"
                                                        Padding="7,3"
                                                        Background="White"
                                                        Visibility="{Binding DataContext.Figure.ShowPanelLabels, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BooleanToVisibility}}">
                                                    <TextBlock Text="{Binding Label, Mode=OneWay}"
                                                               Foreground="{Binding DataContext.Figure.GlobalTextBrush, RelativeSource={RelativeSource AncestorType=Window}}"
                                                               FontFamily="{Binding DataContext.Figure.GlobalFontFamily, RelativeSource={RelativeSource AncestorType=Window}}"
                                                               FontSize="{Binding DataContext.Figure.GlobalFontSizePixels, RelativeSource={RelativeSource AncestorType=Window}}"
                                                               FontWeight="Bold" />
                                                </Border>
                                            <Border Width="14" Height="14" Margin="0,0,5,5" HorizontalAlignment="Right" VerticalAlignment="Bottom" Panel.ZIndex="40" Background="{StaticResource AccentBrush}" BorderBrush="#09151B" BorderThickness="2" Cursor="SizeNWSE" Tag="{Binding}" Visibility="{Binding PanelResizeHandleVisibility}" PreviewMouseLeftButtonDown="FigurePanelResizeHandle_OnMouseLeftButtonDown" PreviewMouseMove="FigurePanelResizeHandle_OnMouseMove" PreviewMouseLeftButtonUp="FigurePanelResizeHandle_OnMouseLeftButtonUp" LostMouseCapture="FigurePanelResizeHandle_OnLostMouseCapture" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            <ItemsControl Width="{Binding Figure.CanvasWidth}"
                                          Height="{Binding Figure.CanvasHeight}"
                                          ItemsSource="{Binding Figure.Guides}"
                                          Panel.ZIndex="90"
                                          IsHitTestVisible="False">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <Canvas />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemContainerStyle>
                                    <Style TargetType="ContentPresenter">
                                        <Setter Property="Canvas.Left" Value="{Binding CanvasLeft}" />
                                        <Setter Property="Canvas.Top" Value="{Binding CanvasTop}" />
                                    </Style>
                                </ItemsControl.ItemContainerStyle>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Width="{Binding LineWidth}"
                                                Height="{Binding LineHeight}">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#A016C7E8" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                            <Setter Property="Background" Value="#E0FFB300" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <ItemsControl Width="{Binding Figure.CanvasWidth}"
                                          Height="{Binding Figure.CanvasHeight}"
                                          ItemsSource="{Binding Figure.Annotations}"
                                          Panel.ZIndex="100"
                                          IsTabStop="False">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <Canvas />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemContainerStyle>
                                    <Style TargetType="ContentPresenter">
                                        <Setter Property="Canvas.Left" Value="{Binding X}" />
                                        <Setter Property="Canvas.Top" Value="{Binding Y}" />
                                        <Setter Property="Panel.ZIndex" Value="{Binding ZIndex}" />
                                    </Style>
                                </ItemsControl.ItemContainerStyle>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Tag="{Binding}"
                                              Cursor="SizeAll"
                                              Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibility}}"
                                              PreviewMouseLeftButtonDown="Annotation_OnMouseLeftButtonDown"
                                              PreviewMouseMove="Annotation_OnMouseMove"
                                              PreviewMouseLeftButtonUp="Annotation_OnMouseLeftButtonUp"
                                              LostMouseCapture="Annotation_OnLostMouseCapture">
                                            <Border Visibility="{Binding TextVisibility}"
                                                    Padding="3"
                                                    Background="Transparent">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="BorderThickness" Value="0" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                                <Setter Property="BorderBrush" Value="#16C7E8" />
                                                                <Setter Property="BorderThickness" Value="3" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding Text}"
                                                           Foreground="{Binding PreviewBrush}"
                                                           FontFamily="{Binding DataContext.Figure.GlobalFontFamily, RelativeSource={RelativeSource AncestorType=Window}}"
                                                           FontSize="{Binding FontSizePixels}">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="FontWeight" Value="Normal" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsBold}" Value="True">
                                                                    <Setter Property="FontWeight" Value="Bold" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                            <Grid Visibility="{Binding ArrowVisibility}">
                                                <Path Data="{Binding ArrowGeometry}"
                                                      Stroke="Transparent"
                                                      StrokeThickness="24"
                                                      Fill="Transparent" />
                                                <Path Data="{Binding ArrowGeometry}"
                                                      Stroke="#16C7E8"
                                                      StrokeThickness="{Binding SelectionStrokeWidthPixels}"
                                                      Fill="#16C7E8"
                                                      IsHitTestVisible="False"
                                                      Visibility="{Binding IsSelected, Converter={StaticResource BooleanToVisibility}}" />
                                                <Path Data="{Binding ArrowGeometry}"
                                                      Stroke="{Binding PreviewBrush}"
                                                      StrokeThickness="{Binding StrokeWidthPixels}"
                                                      Fill="{Binding PreviewBrush}"
                                                      IsHitTestVisible="False" />
                                            </Grid>
                                            <Grid Visibility="{Binding LineVisibility}">
                                                <Path Data="{Binding LineGeometry}"
                                                      Stroke="Transparent"
                                                      StrokeThickness="24" />
                                                <Path Data="{Binding LineGeometry}"
                                                      Stroke="#16C7E8"
                                                      StrokeThickness="{Binding SelectionStrokeWidthPixels}"
                                                      IsHitTestVisible="False"
                                                      Visibility="{Binding IsSelected, Converter={StaticResource BooleanToVisibility}}" />
                                                <Path Data="{Binding LineGeometry}"
                                                      Stroke="{Binding PreviewBrush}"
                                                      StrokeThickness="{Binding StrokeWidthPixels}"
                                                      IsHitTestVisible="False" />
                                            </Grid>
                                            <Grid Width="{Binding ShapeWidth}"
                                                  Height="{Binding ShapeHeight}"
                                                  Background="Transparent"
                                                  Visibility="{Binding ShapeVisibility}">
                                                <Grid Visibility="{Binding RectangleVisibility}">
                                                    <Rectangle Stroke="#16C7E8"
                                                               StrokeThickness="{Binding SelectionStrokeWidthPixels}"
                                                               Fill="Transparent"
                                                               IsHitTestVisible="False"
                                                               Visibility="{Binding IsSelected, Converter={StaticResource BooleanToVisibility}}" />
                                                    <Rectangle Stroke="{Binding PreviewBrush}"
                                                               StrokeThickness="{Binding StrokeWidthPixels}"
                                                               Fill="Transparent"
                                                               IsHitTestVisible="False" />
                                                </Grid>
                                                <Grid Visibility="{Binding EllipseVisibility}">
                                                    <Ellipse Stroke="#16C7E8"
                                                             StrokeThickness="{Binding SelectionStrokeWidthPixels}"
                                                             Fill="Transparent"
                                                             IsHitTestVisible="False"
                                                             Visibility="{Binding IsSelected, Converter={StaticResource BooleanToVisibility}}" />
                                                    <Ellipse Stroke="{Binding PreviewBrush}"
                                                             StrokeThickness="{Binding StrokeWidthPixels}"
                                                             Fill="Transparent"
                                                             IsHitTestVisible="False" />
                                                </Grid>
                                            </Grid>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </ScrollViewer>

                    <StackPanel Visibility="{Binding Figure.EmptyVisibility}" HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False">
                        <TextBlock Text="{Binding Figure.TemplateName}" Foreground="#17202A" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" />
                        <TextBlock Margin="0,10,0,0" Text="{Binding Figure.TemplateDescription}" Foreground="#45515E" FontSize="14" TextWrapping="Wrap" MaxWidth="620" TextAlignment="Center" />
                    </StackPanel>
                </Grid>

                <Border Grid.Row="2"
                        Height="230"
                        Background="#11171F"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="0,1,0,0"
                        Visibility="{Binding MeasurementDockVisibility}">
                    <Grid Margin="12,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="250" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="28" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding SelectedSource.MeasurementCountText}" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" Text="{Binding SelectedSource.MeasurementSummaryText}"
                                   Foreground="#AFC5D2" FontSize="10" VerticalAlignment="Center" TextWrapping="Wrap" />
                        <DataGrid Grid.Row="1"
                                  Margin="0,4,10,0"
                                  ItemsSource="{Binding SelectedSource.Measurements}"
                                  SelectedItem="{Binding SelectedSource.SelectedMeasurement}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  IsReadOnly="True"
                                  HeadersVisibility="Column"
                                  RowHeaderWidth="0"
                                  GridLinesVisibility="Horizontal"
                                  Background="#0F151D"
                                  Foreground="#E8EDF4"
                                  BorderBrush="{StaticResource BorderBrush}"
                                  HorizontalGridLinesBrush="#26313D">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding Number}" Width="42" />
                                <DataGridTextColumn Header="Image" Binding="{Binding SourceName}" Width="120" />
                                <DataGridTextColumn Header="Type" Binding="{Binding TypeText}" Width="72" />
                                <DataGridTextColumn Header="Value" Binding="{Binding ValueText}" Width="112" />
                                <DataGridTextColumn Header="Pixel" Binding="{Binding PixelValueText}" Width="*" />
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,4,0,0">
                            <TextBlock Text="{Binding SelectedSource.SelectedMeasurementStatusText}"
                                       Foreground="#7DD9E9" FontSize="10" TextWrapping="Wrap" />
                            <TextBlock Margin="0,7,0,2"
                                       Text="{Binding SelectedSource.MeasurementHistogramStatusText}"
                                       Foreground="#AFC5D2" FontSize="9" TextTrimming="CharacterEllipsis" />
                            <Border Height="60"
                                    Padding="4"
                                    Background="#0B1016"
                                    BorderBrush="#26313D"
                                    BorderThickness="1"
                                    Visibility="{Binding SelectedSource.MeasurementHistogramVisibility}">
                                <ItemsControl ItemsSource="{Binding SelectedSource.MeasurementHistogramBars}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Rows="1" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid ToolTip="{Binding ToolTip}">
                                                <Border Height="{Binding HeightPixels}"
                                                        Margin="1,0"
                                                        VerticalAlignment="Bottom"
                                                        Background="#FF22C7E8"
                                                        CornerRadius="1,1,0,0" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Border>
                            <Button Margin="0,7,0,0" Command="{Binding DeleteSelectedMeasurementCommand}" Content="删除所选测量" />
                            <Button Margin="0,6,0,0" Command="{Binding CopyMeasurementsCommand}" Content="复制表格" />
                            <Button Margin="0,6,0,0" Command="{Binding ExportMeasurementsCommand}" Style="{StaticResource AccentButtonStyle}" Content="导出 CSV / Excel" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <GridSplitter Grid.Column="3" Width="6" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#263442" ResizeDirection="Columns" ResizeBehavior="PreviousAndNext" ShowsPreview="True" ToolTip="拖动调整检查器宽度" />

            <Border Grid.Column="4" Background="{StaticResource PanelBrush}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="42" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Background="#171E27">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Command="{Binding ShowInspectorTabCommand}" Content="检查器" Background="#171E27" BorderBrush="Transparent" BorderThickness="0,0,0,2" Padding="16,0">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
                                    <Setter Property="Background" Value="#171E27" />
                                    <Setter Property="BorderBrush" Value="Transparent" />
                                    <Setter Property="BorderThickness" Value="0,0,0,2" />
                                    <Setter Property="Padding" Value="16,0" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsLayersTabActive}" Value="False">
                                            <Setter Property="Foreground" Value="#F5FBFF" />
                                            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Grid.Column="1" Command="{Binding ShowLayersTabCommand}" Content="图层" Background="#171E27" BorderBrush="Transparent" BorderThickness="0,0,0,2" Padding="16,0">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
                                    <Setter Property="Background" Value="#171E27" />
                                    <Setter Property="BorderBrush" Value="Transparent" />
                                    <Setter Property="BorderThickness" Value="0,0,0,2" />
                                    <Setter Property="Padding" Value="16,0" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsLayersTabActive}" Value="True">
                                            <Setter Property="Foreground" Value="#F5FBFF" />
                                            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                    <ScrollViewer x:Name="InspectorScrollViewer" Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" CanContentScroll="False" Visibility="{Binding InspectorTabVisibility}">
                    <Grid>
                    <StackPanel Margin="16,16,16,24"
                                IsEnabled="{Binding HasSelection}"
                                Visibility="{Binding CropWorkspaceVisibility}">
                        <TextBlock Text="检查器" FontSize="15" FontWeight="SemiBold" />
                        <TextBlock Margin="0,4,0,0" Text="整数像素 · 非破坏式" Foreground="{StaticResource MutedBrush}" FontSize="10" />

                        <Border Margin="0,14,0,0" Padding="10" Background="#10251E" BorderBrush="#28664F" BorderThickness="1" CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="源图层已锁定" Foreground="#7DE0AF" FontSize="11" FontWeight="SemiBold" />
                                    <TextBlock Margin="0,3,0,0" Text="不会向原图写入像素或元数据" Foreground="#91B8A6" FontSize="9" />
                                </StackPanel>
                                <TextBlock Grid.Column="1" Text="LOCK" Foreground="#7DE0AF" FontSize="9" FontWeight="Bold" VerticalAlignment="Center" />
                            </Grid>
                        </Border>

                        <Border Margin="0,10,0,0" Padding="10" Background="#241B16" BorderBrush="#755137" BorderThickness="1" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="源图版本控制" Foreground="#E7C49C" FontSize="10" FontWeight="SemiBold" />
                                <TextBlock Margin="0,4,0,7"
                                           Text="仅当磁盘文件已被有意更新时使用；操作会显示新旧 SHA-256 并要求再次确认。"
                                           Foreground="#BDA68E"
                                           FontSize="8"
                                           TextWrapping="Wrap" />
                                <Button Command="{Binding AcceptSourceRevisionCommand}" Content="接受当前文件为新版本" />
                            </StackPanel>
                        </Border>

                        <TextBlock Margin="0,22,0,8" Style="{StaticResource SectionTitleStyle}" Text="尺度标定 / CALIBRATION" />
                        <Border Padding="10" Background="#111B23" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding SelectedSource.Calibration.StatusText}" Foreground="#7DD9E9" FontSize="10" TextWrapping="Wrap" />
                                    <TextBlock Grid.Column="1" Margin="8,0,0,0" Text="{Binding SelectedSource.Calibration.OriginText}" Foreground="#AEB9C7" FontSize="9" />
                                </Grid>
                                <Grid Margin="0,10,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="X 尺度 / unit per px" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding SelectedSource.Calibration.UnitsPerPixelX, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Y 尺度 / unit per px" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding SelectedSource.Calibration.UnitsPerPixelY, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </Grid>
                                <Grid Margin="0,10,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="参考线像素长度" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" IsReadOnly="True" Text="{Binding SelectedSource.Calibration.ReferencePixelLength, Mode=OneWay, StringFormat={}{0:0.###}}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="已知真实距离" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding SelectedSource.Calibration.ReferencePhysicalLength, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </Grid>
                                <ComboBox Margin="0,8,0,0"
                                          ItemsSource="{Binding SelectedSource.Calibration.AvailableUnits}"
                                          SelectedItem="{Binding SelectedSource.Calibration.Unit}" />
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="6" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0" Command="{Binding SelectedSource.Calibration.ApplyReferenceCommand}" Style="{StaticResource AccentButtonStyle}" Content="应用参考标定" />
                                    <Button Grid.Column="2" Command="{Binding ApplyCalibrationToFigurePanelsCommand}" Content="同步到拼版" />
                                </Grid>
                                <Button Margin="0,6,0,0" Command="{Binding SelectedSource.Calibration.ClearCalibrationCommand}" Content="清除标定" />
                                <TextBlock Margin="0,8,0,0" Text="{Binding SelectedSource.Calibration.ValidatorText}" Foreground="#AFC5D2" FontSize="9" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <TextBlock Margin="0,22,0,8" Style="{StaticResource SectionTitleStyle}" Text="当前测量" />
                        <Border x:Name="MeasurementInspectorPanel" Padding="10" Background="#111B23" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="{Binding SelectedSource.SelectedMeasurementStatusText}" Foreground="#7DD9E9" FontSize="10" TextWrapping="Wrap" />
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="描边颜色" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding SelectedSource.SelectedMeasurement.StrokeColor, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="线宽 / px" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding SelectedSource.SelectedMeasurement.StrokeWidthPixels, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </Grid>
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="线型" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <ComboBox Margin="0,4,0,0"
                                                  SelectedValue="{Binding SelectedSource.SelectedMeasurement.LineStyle, UpdateSourceTrigger=PropertyChanged}"
                                                  SelectedValuePath="Tag">
                                            <ComboBoxItem Tag="solid" Content="实线" />
                                            <ComboBoxItem Tag="dash" Content="虚线" />
                                            <ComboBoxItem Tag="dot" Content="点线" />
                                            <ComboBoxItem Tag="dash-dot" Content="点划线" />
                                        </ComboBox>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="端点大小 / px" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding SelectedSource.SelectedMeasurement.MarkerSizePixels, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </Grid>
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="ROI 填充透明度 / %" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding SelectedSource.SelectedMeasurement.FillOpacityPercent, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" VerticalAlignment="Bottom">
                                        <CheckBox IsChecked="{Binding SelectedSource.SelectedMeasurement.ShowMarkers}" Content="显示端点" />
                                        <CheckBox Margin="0,6,0,0" IsChecked="{Binding SelectedSource.SelectedMeasurement.ShowLabel}" Content="显示数值标签" />
                                    </StackPanel>
                                </Grid>
                                <Grid Margin="0,9,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <CheckBox IsChecked="{Binding SelectedSource.SelectedMeasurement.IsVisible}" Content="图层可见" />
                                    <CheckBox Grid.Column="1" IsChecked="{Binding SelectedSource.SelectedMeasurement.IsLocked}" Content="锁定图层" />
                                </Grid>
                                <TextBlock Margin="0,7,0,0" Text="选中后拖动图形可移动；拖动黄色端点手柄可改大小。" Foreground="{StaticResource MutedBrush}" FontSize="8" TextWrapping="Wrap" />
                                <Button Margin="0,8,0,0" Command="{Binding DeleteSelectedMeasurementCommand}" Content="删除当前测量" />
                                <Button Margin="0,6,0,0"
                                        Command="{Binding AnalyzeIntensityProfileCommand}"
                                        Style="{StaticResource AccentButtonStyle}"
                                        Content="从原始像素生成强度剖面" />
                                <TextBlock Margin="0,6,0,0"
                                           Text="{Binding IntensityProfileStatusText}"
                                           Foreground="{StaticResource MutedBrush}"
                                           FontSize="8"
                                           TextWrapping="Wrap" />
                                <Border Margin="0,8,0,0"
                                        Padding="7"
                                        Background="#0B131A"
                                        BorderBrush="#315365"
                                        BorderThickness="1"
                                        CornerRadius="3"
                                        Visibility="{Binding IntensityProfileVisibility}">
                                    <Viewbox Stretch="Uniform" StretchDirection="DownOnly">
                                        <Canvas Width="300" Height="100">
                                            <Line X1="0" Y1="99" X2="300" Y2="99" Stroke="#4E6474" StrokeThickness="1" />
                                            <Line X1="0" Y1="0" X2="0" Y2="100" Stroke="#4E6474" StrokeThickness="1" />
                                            <Polyline Points="{Binding IntensityProfilePoints}"
                                                      Stroke="#22C7E8"
                                                      StrokeThickness="2"
                                                      StrokeLineJoin="Round" />
                                        </Canvas>
                                    </Viewbox>
                                </Border>
                            </StackPanel>
                        </Border>

                        <TextBlock Margin="0,22,0,8" Style="{StaticResource SectionTitleStyle}" Text="智能辅助区域分析 / 可审计规则" />
                        <Border Padding="10" Background="#171A21" BorderBrush="#66543A" BorderThickness="1" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="只生成候选，不修改原图；候选必须人工接受后才写入测量表。"
                                           Foreground="#E8C989" FontSize="9" TextWrapping="Wrap" />
                                <ComboBox Margin="0,8,0,0"
                                          ItemsSource="{Binding AssistedRegionModes}"
                                          DisplayMemberPath="Label"
                                          SelectedValuePath="Mode"
                                          SelectedValue="{Binding AssistedRegionMode}" />
                                <CheckBox Margin="0,8,0,0"
                                          IsChecked="{Binding UseAutomaticRegionThreshold}"
                                          Content="自动阈值（Otsu，可复现）" />
                                <Grid Margin="0,7,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="手动阈值 / %" Foreground="{StaticResource MutedBrush}" FontSize="8" />
                                        <TextBox Margin="0,3,0,0"
                                                 Text="{Binding RegionThresholdPercent, UpdateSourceTrigger=PropertyChanged}"
                                                 ToolTip="关闭自动阈值后使用此值" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="最小面积 / px²" Foreground="{StaticResource MutedBrush}" FontSize="8" />
                                        <TextBox Margin="0,3,0,0" Text="{Binding MinimumRegionAreaPixels, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </Grid>
                                <Button Margin="0,8,0,0"
                                        Command="{Binding AnalyzeAssistedRegionsCommand}"
                                        Style="{StaticResource AccentButtonStyle}"
                                        Content="分析当前裁剪 ROI" />
                                <TextBlock Margin="0,7,0,0"
                                           Text="{Binding AssistedRegionStatusText}"
                                           Foreground="#D9C29A"
                                           FontSize="8"
                                           TextWrapping="Wrap" />
                                <StackPanel Visibility="{Binding AssistedRegionResultsVisibility}">
                                    <TextBlock Margin="0,7,0,4"
                                               Text="{Binding AssistedRegionDecisionText}"
                                               Foreground="#AFC5D2"
                                               FontSize="8" />
                                    <DataGrid Height="150"
                                              ItemsSource="{Binding AssistedRegions}"
                                              SelectedItem="{Binding SelectedAssistedRegion}"
                                              AutoGenerateColumns="False"
                                              CanUserAddRows="False"
                                              CanUserDeleteRows="False"
                                              HeadersVisibility="Column"
                                              RowHeaderWidth="0"
                                              Background="#0D1319"
                                              BorderBrush="{StaticResource BorderBrush}">
                                        <DataGrid.Columns>
                                            <DataGridCheckBoxColumn Header="采用" Binding="{Binding IsAccepted, Mode=TwoWay}" Width="42" />
                                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="34" IsReadOnly="True" />
                                            <DataGridTextColumn Header="等效尺寸" Binding="{Binding SizeText}" Width="*" IsReadOnly="True" />
                                            <DataGridTextColumn Header="状态" Binding="{Binding DecisionText}" Width="78" IsReadOnly="True" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                    <UniformGrid Margin="0,7,0,0" Columns="2" Rows="2">
                                        <Button Margin="0,0,4,4" Command="{Binding AcceptAllAssistedRegionsCommand}" Content="全部接受" />
                                        <Button Margin="4,0,0,4" Command="{Binding RejectSelectedAssistedRegionCommand}" Content="拒绝所选" />
                                        <Button Margin="0,4,4,0" Command="{Binding CommitAcceptedAssistedRegionsCommand}" Style="{StaticResource AccentButtonStyle}" Content="写入测量表" />
                                        <Button Margin="4,4,0,0" Command="{Binding ClearAssistedRegionsCommand}" Content="清除候选" />
                                    </UniformGrid>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <TextBlock Margin="0,22,0,8" Style="{StaticResource SectionTitleStyle}" Text="裁剪区域 / PX" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="X" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                <TextBox Margin="0,4,0,0" Text="{Binding Crop.X, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Y" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                <TextBox Margin="0,4,0,0" Text="{Binding Crop.Y, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                        </Grid>
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="宽度" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                <TextBox Margin="0,4,0,0" Text="{Binding Crop.Width, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="高度" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                <TextBox Margin="0,4,0,0" Text="{Binding Crop.Height, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                        </Grid>
                        <TextBlock Margin="0,9,0,0" Text="{Binding Crop.ValidationMessage}" Foreground="#7DD9E9" FontSize="10" TextWrapping="Wrap" />
                        <CheckBox Margin="0,10,0,0"
                                  IsChecked="{Binding LockCropSizeAcrossSources}"
                                  Content="切换源图像时保持相同宽高"
                                  ToolTip="用于在一批图像上沿用同一裁剪尺寸，并分别自由选择位置" />

                        <TextBlock Margin="0,22,0,8" Style="{StaticResource SectionTitleStyle}" Text="对齐到源图像" />
                        <UniformGrid Columns="3" Rows="2">
                            <Button Margin="0,0,5,5" Padding="7" Command="{Binding Crop.AlignLeftCommand}" Content="左" ToolTip="左对齐" />
                            <Button Margin="0,0,5,5" Padding="7" Command="{Binding Crop.AlignHorizontalCenterCommand}" Content="水平中" ToolTip="水平居中" />
                            <Button Margin="0,0,0,5" Padding="7" Command="{Binding Crop.AlignRightCommand}" Content="右" ToolTip="右对齐" />
                            <Button Margin="0,0,5,0" Padding="7" Command="{Binding Crop.AlignTopCommand}" Content="上" ToolTip="上对齐" />
                            <Button Margin="0,0,5,0" Padding="7" Command="{Binding Crop.AlignVerticalCenterCommand}" Content="垂直中" ToolTip="垂直居中" />
                            <Button Padding="7" Command="{Binding Crop.AlignBottomCommand}" Content="下" ToolTip="下对齐" />
                        </UniformGrid>

                        <TextBlock Margin="0,22,0,8" Style="{StaticResource SectionTitleStyle}" Text="图层" />
                        <Border Background="{StaticResource PanelRaisedBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                            <StackPanel>
                                <Grid Margin="10,9">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <CheckBox IsChecked="{Binding IsCropOverlayVisible}" />
                                    <TextBlock Grid.Column="1" Margin="8,0,0,0" Text="裁剪框" VerticalAlignment="Center" FontSize="11" />
                                    <TextBlock Grid.Column="2" Text="OVERLAY" Foreground="{StaticResource MutedBrush}" FontSize="8" VerticalAlignment="Center" />
                                </Grid>
                                <Border Height="1" Background="{StaticResource BorderBrush}" />
                                <Grid Margin="10,9">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <CheckBox IsChecked="True" IsEnabled="False" />
                                    <TextBlock Grid.Column="1" Margin="8,0,0,0" Text="{Binding SelectedSource.DisplayName, FallbackValue=源图像}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" FontSize="11" />
                                    <TextBlock Grid.Column="2" Text="LOCK" Foreground="#70D6A4" FontSize="8" FontWeight="Bold" VerticalAlignment="Center" />
                                </Grid>
                            </StackPanel>
                        </Border>

                        <TextBlock Margin="0,22,0,8" Style="{StaticResource SectionTitleStyle}" Text="输出" />
                        <Button Command="{Binding ExportCropCommand}"
                                Style="{StaticResource AccentButtonStyle}"
                                Content="导出当前裁剪" />
                        <TextBlock Margin="0,8,0,0"
                                   Text="首版仅写入全新文件；不会覆盖源图或任何已有文件。TIFF 为推荐格式。"
                                   Foreground="{StaticResource MutedBrush}"
                                   FontSize="9"
                                   TextWrapping="Wrap" />

                        <TextBlock Margin="0,22,0,8" Style="{StaticResource SectionTitleStyle}" Text="批量裁剪队列" />
                        <ListBox Height="150"
                                 ItemsSource="{Binding BatchCropQueue}"
                                 SelectedItem="{Binding SelectedBatchCrop}"
                                 Background="{StaticResource PanelRaisedBrush}"
                                 BorderBrush="{StaticResource BorderBrush}"
                                 BorderThickness="1">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="6,5">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <TextBlock Text="{Binding DisplayName}" FontSize="10" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Grid.Row="1" Text="{Binding CropSummary}" Foreground="{StaticResource MutedBrush}" FontSize="8" />
                                        <TextBlock Grid.Row="2" Text="{Binding StatusText}" Foreground="#7DD9E9" FontSize="8" TextTrimming="CharacterEllipsis" />
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <Grid Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="6" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Command="{Binding AddCurrentCropToBatchQueueCommand}" Content="加入队列" />
                            <Button Grid.Column="2" Command="{Binding RemoveSelectedBatchCropCommand}" Content="移除所选" />
                        </Grid>
                        <Grid Margin="0,6,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="6" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Command="{Binding ClearBatchCropQueueCommand}" Content="清空队列" />
                            <Button Grid.Column="2" Command="{Binding ExportBatchCropsCommand}" Style="{StaticResource AccentButtonStyle}" Content="导出批量裁剪" />
                        </Grid>
                        <TextBlock Margin="0,7,0,0" Text="逐项复核源文件并输出全新 TIFF；失败项会保留在队列中。" Foreground="{StaticResource MutedBrush}" FontSize="9" TextWrapping="Wrap" />
                    </StackPanel>

                    <StackPanel Margin="16,16,16,24" Visibility="{Binding FigureWorkspaceVisibility}">
                        <TextBlock Text="拼版检查器" FontSize="15" FontWeight="SemiBold" />
                        <TextBlock Margin="0,4,0,0" Text="模板布局 · 原始像素导出" Foreground="{StaticResource MutedBrush}" FontSize="10" />

                        <Border Margin="0,14,0,0" Padding="11" Background="#16232D" BorderBrush="#315365" BorderThickness="1" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="模板" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                <Grid Margin="0,5,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <ComboBox Grid.Column="0"
                                              ItemsSource="{Binding AvailableTemplates}"
                                              SelectedItem="{Binding SelectedFigureTemplate}"
                                              DisplayMemberPath="Name"
                                              IsEnabled="{Binding IsTemplateSelectionEnabled}"
                                              ToolTip="已有面板时请先清空拼版，再切换模板" />
                                    <Button Grid.Column="2" Command="{Binding ImportTemplateCommand}" Content="导入" ToolTip="导入并安装用户模板 JSON" />
                                </Grid>
                                <TextBlock Text="{Binding Figure.TemplateName}" Foreground="#8CE5F4" FontSize="12" FontWeight="SemiBold" TextWrapping="Wrap" />
                                <TextBlock Margin="0,5,0,0" Text="{Binding Figure.CanvasSizeText}" Foreground="#C0CBD6" FontSize="10" />
                                <TextBlock Margin="0,5,0,0" Text="{Binding Figure.TemplateDescription}" Foreground="{StaticResource MutedBrush}" FontSize="9" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <TextBlock Margin="0,18,0,8" Style="{StaticResource SectionTitleStyle}" Text="画布与面板编号" />
                        <Border Padding="10" Background="{StaticResource PanelRaisedBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="背景颜色 / HEX" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                <Grid Margin="0,5,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <ComboBox IsEditable="True"
                                              Text="{Binding Figure.BackgroundColor, UpdateSourceTrigger=PropertyChanged}">
                                        <ComboBoxItem Content="#FFFFFFFF" />
                                        <ComboBoxItem Content="#FF000000" />
                                        <ComboBoxItem Content="#FFF5F5F2" />
                                        <ComboBoxItem Content="#00FFFFFF" />
                                    </ComboBox>
                                    <Button Grid.Column="2" Padding="8,5" Command="{Binding Figure.ResetBackgroundCommand}" Content="白色" />
                                </Grid>
                                <TextBlock Margin="0,5,0,0"
                                           Text="{Binding Figure.BackgroundValidationMessage}"
                                           Foreground="#80DDEA"
                                           FontSize="8"
                                           TextWrapping="Wrap" />
                                <CheckBox Margin="0,10,0,0" IsChecked="{Binding Figure.ShowPanelLabels}" Content="预览与导出面板编号" />
                                <CheckBox Margin="0,5,0,0" IsChecked="{Binding Figure.AutoPanelLabelsEnabled}" Content="新增或删除面板时自动编号" />
                                <Grid Margin="0,7,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="118" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="编号序列" Foreground="{StaticResource MutedBrush}" FontSize="9" VerticalAlignment="Center" />
                                    <ComboBox Grid.Column="2"
                                              SelectedValue="{Binding Figure.PanelLabelSequence}"
                                              SelectedValuePath="Tag">
                                        <ComboBoxItem Tag="lowercase" Content="a, b, c…" />
                                        <ComboBoxItem Tag="uppercase" Content="A, B, C…" />
                                        <ComboBoxItem Tag="numeric" Content="1, 2, 3…" />
                                    </ComboBox>
                                </Grid>
                                <Button Margin="0,7,0,0" Command="{Binding Figure.RenumberPanelLabelsCommand}" Content="按画布位置重新编号" />
                                <TextBlock Margin="0,5,0,0" Text="{Binding Figure.PanelLabelSettingsText}" Foreground="{StaticResource MutedBrush}" FontSize="8" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <TextBlock Margin="0,18,0,8" Style="{StaticResource SectionTitleStyle}" Text="全局图样式" />
                        <Border Padding="10" Background="{StaticResource PanelRaisedBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="字体家族" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                <TextBox Margin="0,4,0,0" Text="{Binding Figure.GlobalFontFamily, UpdateSourceTrigger=PropertyChanged}" />
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="字号 / pt" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding Figure.GlobalFontSizePt, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="线宽 / pt" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding Figure.GlobalStrokeWidthPt, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </Grid>
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="6" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="6" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="文字" Foreground="{StaticResource MutedBrush}" FontSize="8" />
                                        <TextBox Margin="0,3,0,0" Text="{Binding Figure.GlobalTextColor, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="图形" Foreground="{StaticResource MutedBrush}" FontSize="8" />
                                        <TextBox Margin="0,3,0,0" Text="{Binding Figure.GlobalShapeColor, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="4">
                                        <TextBlock Text="比例尺" Foreground="{StaticResource MutedBrush}" FontSize="8" />
                                        <TextBox Margin="0,3,0,0" Text="{Binding Figure.GlobalScaleBarColor, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </Grid>
                                <Button Margin="0,9,0,0"
                                        Command="{Binding Figure.ApplyGlobalStyleCommand}"
                                        Style="{StaticResource AccentButtonStyle}"
                                        Content="一键统一全部标注" />
                                <TextBlock Margin="0,5,0,0" Text="{Binding Figure.GlobalStyleStatusText}" Foreground="{StaticResource MutedBrush}" FontSize="8" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <TextBlock Margin="0,18,0,8" Style="{StaticResource SectionTitleStyle}" Text="科研颜色字典" />
                        <Border Padding="10" Background="{StaticResource PanelRaisedBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="同一物理对象在全项目复用同一颜色，并进行红绿色觉缺陷近似检查。"
                                           Foreground="{StaticResource MutedBrush}" FontSize="8" TextWrapping="Wrap" />
                                <ListBox Margin="0,8,0,0"
                                         MaxHeight="210"
                                         ItemsSource="{Binding Figure.ScientificColors}"
                                         SelectedItem="{Binding Figure.SelectedScientificColor}"
                                         Background="#0D151C"
                                         BorderBrush="{StaticResource BorderBrush}">
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                            <Setter Property="Padding" Value="3" />
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="22" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="92" />
                                                </Grid.ColumnDefinitions>
                                                <Border Width="16" Height="16" Background="{Binding ColorBrush}" BorderBrush="#8090A0" BorderThickness="1" CornerRadius="2" />
                                                <TextBox Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                                                <TextBox Grid.Column="3" Text="{Binding Color, UpdateSourceTrigger=PropertyChanged}" />
                                            </Grid>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                                <UniformGrid Margin="0,8,0,0" Columns="3" Rows="1">
                                    <Button Margin="0,0,4,0" Command="{Binding Figure.AddScientificColorCommand}" Content="新增" />
                                    <Button Margin="4,0" Command="{Binding Figure.RemoveSelectedScientificColorCommand}" Content="删除" />
                                    <Button Margin="4,0,0,0" Command="{Binding Figure.ApplySelectedScientificColorCommand}" Style="{StaticResource AccentButtonStyle}" Content="应用" ToolTip="应用到所选标注；未选择标注时设为全局图形颜色" />
                                </UniformGrid>
                                <TextBlock Margin="0,7,0,0"
                                           Text="{Binding Figure.ScientificColorStatusText}"
                                           Foreground="#80DDEA"
                                           FontSize="8"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <Grid Margin="0,18,0,8">
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="面板图层" />
                            <TextBlock HorizontalAlignment="Right" Text="{Binding Figure.PanelCountText}" Foreground="{StaticResource MutedBrush}" FontSize="10" />
                        </Grid>
                        <ListBox MinHeight="120"
                                 MaxHeight="300"
                                 Background="{StaticResource PanelRaisedBrush}"
                                 BorderBrush="{StaticResource BorderBrush}"
                                 BorderThickness="1"
                                 ItemsSource="{Binding Figure.Panels}"
                                 SelectionMode="Extended"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="7,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <CheckBox IsChecked="{Binding IsVisible}" ToolTip="显示/隐藏图层" />
                                        <StackPanel Grid.Column="1" Margin="8,0">
                                            <TextBlock FontSize="11" FontWeight="SemiBold">
                                                <Run Text="{Binding Label, Mode=OneWay}" /><Run Text=" · " /><Run Text="{Binding RoleDisplayName, Mode=OneWay}" />
                                            </TextBlock>
                                            <TextBlock Text="{Binding Source.DisplayName}" Foreground="{StaticResource MutedBrush}" FontSize="9" TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                        <CheckBox Grid.Column="2" IsChecked="{Binding IsLocked}" Content="锁" FontSize="9" ToolTip="锁定后不能在画布拖动" />
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <UniformGrid Margin="0,8,0,0" Columns="3">
                            <Button Margin="0,0,5,0" Padding="7" Command="{Binding Figure.MoveLayerUpCommand}" Content="上移" />
                            <Button Margin="0,0,5,0" Padding="7" Command="{Binding Figure.MoveLayerDownCommand}" Content="下移" />
                            <Button Padding="7" Command="{Binding Figure.RemoveSelectedCommand}" Content="移除所选" />
                        </UniformGrid>
                        <UniformGrid Margin="0,6,0,0" Columns="2">
                            <Button Margin="0,0,5,0" Padding="6" Command="{Binding Figure.SelectAllPanelsCommand}" Content="全选面板" />
                            <Button Padding="6" Command="{Binding Figure.ClearPanelSelectionCommand}" Content="取消选择" />
                        </UniformGrid>

                        <TextBlock Margin="0,20,0,8" Style="{StaticResource SectionTitleStyle}" Text="选中面板" />
                        <Border Padding="10" Background="{StaticResource PanelRaisedBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="{Binding Figure.SelectedPanel.RoleDisplayName, FallbackValue=未选择面板}" FontWeight="SemiBold" />
                                <TextBlock Margin="0,8,0,3" Text="面板编号" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                <TextBox Text="{Binding Figure.SelectedPanel.Label, UpdateSourceTrigger=PropertyChanged}" MaxLength="16" />
                                <TextBlock Margin="0,5,0,0" Text="{Binding Figure.SelectedPanel.FrameStatusText}" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                <StackPanel Margin="0,5,0,0" Visibility="{Binding Figure.SelectedPanel.FrameSelectionVisibility}">
                                    <TextBlock Text="多页图像帧（从 1 开始）" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                    <TextBox Margin="0,3,0,0" Text="{Binding Figure.SelectedPanel.FrameNumber, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                                <TextBlock Margin="0,5,0,0" Text="{Binding Figure.SelectedPanel.EffectiveDpiText}" Foreground="#80DDEA" FontSize="10" />
                                <TextBlock Margin="0,4,0,0" Text="{Binding Figure.SelectedPanel.HelpText}" Foreground="{StaticResource MutedBrush}" FontSize="9" TextWrapping="Wrap" />
                                <Button Margin="0,8,0,0" Command="{Binding ReplaceSelectedPanelSourceCommand}" Content="用当前源图替换面板" />
                                <Grid Margin="0,10,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="X" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding Figure.SelectedPanel.X, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Y" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding Figure.SelectedPanel.Y, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </Grid>

                                <TextBlock Margin="0,12,0,6" Text="尺寸与比例 / 画布 px" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="宽度" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding Figure.SelectedPanel.Width, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="高度" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding Figure.SelectedPanel.Height, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </Grid>
                                <CheckBox Margin="0,8,0,0"
                                          IsChecked="{Binding Figure.SelectedPanel.IsAspectRatioLocked}"
                                          Content="锁定源图比例（等比缩放）"
                                          ToolTip="锁定后修改宽度或高度会同步调整另一边；关闭后可自由输入宽高。" />
                                <Grid Margin="0,7,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="90" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="整体缩放 / %" Foreground="{StaticResource MutedBrush}" FontSize="9" VerticalAlignment="Center" />
                                    <TextBox Grid.Column="2"
                                             Text="{Binding Figure.SelectedPanel.ScalePercent, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:0.#}}"
                                             ToolTip="按源图比例同时缩放宽高，范围 1%—1000%。" />
                                </Grid>
                                <TextBlock Margin="0,5,0,0" Text="{Binding Figure.SelectedPanel.AspectRatioText}" Foreground="{StaticResource MutedBrush}" FontSize="8" />
                                <TextBlock Margin="0,2,0,0" Text="{Binding Figure.SelectedPanel.SizeStatusText}" Foreground="#80DDEA" FontSize="9" />

                                <TextBlock Margin="0,12,0,6" Text="对齐到画布" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                <UniformGrid Columns="3" Rows="2">
                                    <Button Margin="0,0,5,5" Padding="6" Command="{Binding Figure.AlignPanelLeftCommand}" Content="左" />
                                    <Button Margin="0,0,5,5" Padding="6" Command="{Binding Figure.AlignPanelHorizontalCenterCommand}" Content="水平中" />
                                    <Button Margin="0,0,0,5" Padding="6" Command="{Binding Figure.AlignPanelRightCommand}" Content="右" />
                                    <Button Margin="0,0,5,0" Padding="6" Command="{Binding Figure.AlignPanelTopCommand}" Content="上" />
                                    <Button Margin="0,0,5,0" Padding="6" Command="{Binding Figure.AlignPanelVerticalCenterCommand}" Content="垂直中" />
                                    <Button Padding="6" Command="{Binding Figure.AlignPanelBottomCommand}" Content="下" />
                                </UniformGrid>

                                <Border Margin="0,12,0,0"
                                        Padding="9"
                                        Background="#10212A"
                                        BorderBrush="#245A68"
                                        BorderThickness="1"
                                        CornerRadius="3"
                                        Visibility="{Binding Figure.MultiplePanelSelectionVisibility}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Figure.SelectedPanelCountText}" Foreground="#8CE5F4" FontSize="10" FontWeight="SemiBold" />
                                        <TextBlock Margin="0,4,0,7" Text="所选面板相互对齐；锁定面板作为参照但不会移动。" Foreground="{StaticResource MutedBrush}" FontSize="8" TextWrapping="Wrap" />
                                        <UniformGrid Columns="3" Rows="2">
                                            <Button Margin="0,0,5,5" Padding="5" Command="{Binding Figure.AlignSelectionLeftCommand}" Content="齐左" />
                                            <Button Margin="0,0,5,5" Padding="5" Command="{Binding Figure.AlignSelectionHorizontalCenterCommand}" Content="横中" />
                                            <Button Margin="0,0,0,5" Padding="5" Command="{Binding Figure.AlignSelectionRightCommand}" Content="齐右" />
                                            <Button Margin="0,0,5,0" Padding="5" Command="{Binding Figure.AlignSelectionTopCommand}" Content="齐上" />
                                            <Button Margin="0,0,5,0" Padding="5" Command="{Binding Figure.AlignSelectionVerticalCenterCommand}" Content="纵中" />
                                            <Button Padding="5" Command="{Binding Figure.AlignSelectionBottomCommand}" Content="齐下" />
                                        </UniformGrid>
                                        <UniformGrid Margin="0,7,0,0" Columns="2">
                                            <Button Margin="0,0,5,0" Padding="5" Command="{Binding Figure.DistributeSelectionHorizontallyCommand}" Content="水平等距" />
                                            <Button Padding="5" Command="{Binding Figure.DistributeSelectionVerticallyCommand}" Content="垂直等距" />
                                        </UniformGrid>
                                        <UniformGrid Margin="0,7,0,0" Columns="2">
                                            <Button Margin="0,0,5,0" Padding="5" Command="{Binding Figure.LinkSelectedPanelCropsCommand}" Content="关联裁剪" ToolTip="所选面板必须来自同一源图；之后替换其中一个裁剪会同步到同组面板" />
                                            <Button Padding="5" Command="{Binding Figure.UnlinkSelectedPanelCropsCommand}" Content="解除关联" />
                                        </UniformGrid>
                                        <TextBlock Margin="0,9,0,5"
                                                   Text="匹配主选面板（最后选中的高亮面板）"
                                                   Foreground="{StaticResource MutedBrush}"
                                                   FontSize="8" />
                                        <UniformGrid Columns="4">
                                            <Button Margin="0,0,4,0" Padding="4" Command="{Binding Figure.MatchSelectionWidthCommand}" Content="同宽" />
                                            <Button Margin="0,0,4,0" Padding="4" Command="{Binding Figure.MatchSelectionHeightCommand}" Content="同高" />
                                            <Button Margin="0,0,4,0" Padding="4" Command="{Binding Figure.MatchSelectionFrameCommand}" Content="同框" />
                                            <Button Padding="4" Command="{Binding Figure.MatchSelectionAspectRatioCommand}" Content="同比" ToolTip="保持各自宽度，将宽高比匹配到主选面板" />
                                        </UniformGrid>
                                        <Grid Margin="0,8,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="8" />
                                                <ColumnDefinition Width="90" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="精确边界间距 / px" Foreground="{StaticResource MutedBrush}" FontSize="9" VerticalAlignment="Center" />
                                            <TextBox Grid.Column="2" Text="{Binding Figure.ExactSpacingPixels, UpdateSourceTrigger=PropertyChanged}" />
                                        </Grid>
                                        <UniformGrid Margin="0,6,0,0" Columns="2">
                                            <Button Margin="0,0,5,0" Padding="5" Command="{Binding Figure.SetHorizontalSpacingCommand}" Content="应用水平间距" />
                                            <Button Padding="5" Command="{Binding Figure.SetVerticalSpacingCommand}" Content="应用垂直间距" />
                                        </UniformGrid>
                                        <TextBlock Margin="0,5,0,0" Text="{Binding Figure.ExactSpacingStatusText}" Foreground="{StaticResource MutedBrush}" FontSize="8" TextWrapping="Wrap" />
                                    </StackPanel>
                                </Border>

                                <Border Margin="0,14,0,0" Padding="9" Background="#101820" BorderBrush="#334454" BorderThickness="1" CornerRadius="3">
                                    <StackPanel>
                                        <CheckBox IsChecked="{Binding Figure.SelectedPanel.ShowScaleBar}"
                                                  Content="显示经过校准的比例尺" />
                                        <TextBlock Margin="0,6,0,0" Text="{Binding Figure.SelectedPanel.CropLinkStatusText}" Foreground="{StaticResource MutedBrush}" FontSize="8" />
                                        <Button Margin="0,7,0,0" Command="{Binding Figure.CreateInsetCommand}" Content="创建 Inset 局部放大" />
                                        <Grid Margin="0,10,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="8" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="物理尺寸 / 像素" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                                <TextBox Margin="0,4,0,0"
                                                         Text="{Binding Figure.SelectedPanel.PhysicalUnitsPerSourcePixel, UpdateSourceTrigger=PropertyChanged}" />
                                            </StackPanel>
                                            <StackPanel Grid.Column="2">
                                                <TextBlock Text="比例尺长度" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                                <TextBox Margin="0,4,0,0"
                                                         Text="{Binding Figure.SelectedPanel.ScaleBarPhysicalLength, UpdateSourceTrigger=PropertyChanged}" />
                                            </StackPanel>
                                        </Grid>
                                        <Grid Margin="0,8,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="8" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="单位" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                                <ComboBox Margin="0,4,0,0"
                                                          IsEditable="True"
                                                          Text="{Binding Figure.SelectedPanel.ScaleBarUnit, UpdateSourceTrigger=PropertyChanged}">
                                                    <ComboBoxItem Content="nm" />
                                                    <ComboBoxItem Content="µm" />
                                                    <ComboBoxItem Content="mm" />
                                                </ComboBox>
                                            </StackPanel>
                                            <CheckBox Grid.Column="2"
                                                      VerticalAlignment="Bottom"
                                                      Margin="0,0,0,6"
                                                      IsChecked="{Binding Figure.SelectedPanel.ScaleBarShowLabel}"
                                                      Content="显示数值标签" />
                                        </Grid>
                                        <TextBlock Margin="0,8,0,0"
                                                   Text="{Binding Figure.SelectedPanel.ScaleBarStatusText}"
                                                   Foreground="#80DDEA"
                                                   FontSize="9"
                                                   TextWrapping="Wrap" />
                                        <TextBlock Margin="0,5,0,0"
                                                   Text="校准值应来自显微镜元数据或实验记录；软件不会猜测物理尺度。"
                                                   Foreground="{StaticResource MutedBrush}"
                                                   FontSize="8"
                                                   TextWrapping="Wrap" />
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>

                        <Grid Margin="0,20,0,8">
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="非破坏性图像处理" />
                            <TextBlock HorizontalAlignment="Right" Text="显示层" Foreground="{StaticResource MutedBrush}" FontSize="10" />
                        </Grid>
                        <Border Padding="10" Background="{StaticResource PanelRaisedBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="仅记录参数，不改写源图；导出时从原始像素重新渲染。" Foreground="{StaticResource MutedBrush}" FontSize="8" TextWrapping="Wrap" />
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="亮度 (-100—+100)" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <Slider Margin="0,4,0,0"
                                                Minimum="-100"
                                                Maximum="100"
                                                SmallChange="1"
                                                LargeChange="10"
                                                IsSnapToTickEnabled="False"
                                                Height="18"
                                                Foreground="{StaticResource AccentBrush}"
                                                Value="{Binding Figure.SelectedPanel.BrightnessPercent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                        <TextBox Margin="0,3,0,0" Text="{Binding Figure.SelectedPanel.BrightnessPercent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="对比度 (-100—+100)" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <Slider Margin="0,4,0,0"
                                                Minimum="-100"
                                                Maximum="100"
                                                SmallChange="1"
                                                LargeChange="10"
                                                IsSnapToTickEnabled="False"
                                                Height="18"
                                                Foreground="{StaticResource AccentBrush}"
                                                Value="{Binding Figure.SelectedPanel.ContrastPercent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                        <TextBox Margin="0,3,0,0" Text="{Binding Figure.SelectedPanel.ContrastPercent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </Grid>
                                <Grid Margin="0,7,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Gamma (0.1—10)" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,3,0,0" Text="{Binding Figure.SelectedPanel.Gamma, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="黑点 (0—1)" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,3,0,0" Text="{Binding Figure.SelectedPanel.BlackPoint, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </Grid>
                                <Grid Margin="0,7,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="白点 (0—1)" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,3,0,0" Text="{Binding Figure.SelectedPanel.WhitePoint, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="通道" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <ComboBox Margin="0,3,0,0" IsEditable="True" Text="{Binding Figure.SelectedPanel.Channel, UpdateSourceTrigger=PropertyChanged}">
                                            <ComboBoxItem Content="rgb" />
                                            <ComboBoxItem Content="red" />
                                            <ComboBoxItem Content="green" />
                                            <ComboBoxItem Content="blue" />
                                            <ComboBoxItem Content="alpha" />
                                        </ComboBox>
                                    </StackPanel>
                                </Grid>
                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                    <CheckBox IsChecked="{Binding Figure.SelectedPanel.Grayscale}" Content="灰度化" />
                                    <CheckBox Margin="14,0,0,0" IsChecked="{Binding Figure.SelectedPanel.Invert}" Content="反相" />
                                </StackPanel>
                                <TextBlock Margin="0,8,0,0" Text="{Binding Figure.SelectedPanel.AdjustmentStatusText}" Foreground="#80DDEA" FontSize="9" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                        <Grid Margin="0,20,0,8">
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="参考线与吸附" />
                            <TextBlock HorizontalAlignment="Right" Text="{Binding Figure.GuideCountText}" Foreground="{StaticResource MutedBrush}" FontSize="10" />
                        </Grid>
                        <CheckBox IsChecked="{Binding Figure.IsSnappingEnabled}" Content="拖动面板时启用吸附" />
                        <Grid Margin="0,7,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="8" />
                                <ColumnDefinition Width="90" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="吸附阈值 / px" Foreground="{StaticResource MutedBrush}" FontSize="9" VerticalAlignment="Center" />
                            <TextBox Grid.Column="2" Text="{Binding Figure.SnapTolerancePixels, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>
                        <TextBlock Margin="0,5,0,0" Text="{Binding Figure.SnapSettingsText}" Foreground="#80DDEA" FontSize="8" />
                        <UniformGrid Margin="0,8,0,0" Columns="2">
                            <Button Margin="0,0,5,0" Command="{Binding Figure.AddVerticalGuideCommand}" Content="＋ 垂直参考线" />
                            <Button Command="{Binding Figure.AddHorizontalGuideCommand}" Content="＋ 水平参考线" />
                        </UniformGrid>
                        <ListBox Margin="0,8,0,0"
                                 MinHeight="60"
                                 MaxHeight="140"
                                 Background="{StaticResource PanelRaisedBrush}"
                                 BorderBrush="{StaticResource BorderBrush}"
                                 BorderThickness="1"
                                 ItemsSource="{Binding Figure.Guides}"
                                 SelectedItem="{Binding Figure.SelectedGuide}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="7,5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Summary}" FontSize="10" />
                                        <TextBlock Grid.Column="1" Text="锁" Foreground="#70D6A4" FontSize="9"
                                                   Visibility="{Binding IsLocked, Converter={StaticResource BooleanToVisibility}}" />
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <Border Margin="0,8,0,0"
                                Padding="9"
                                Background="{StaticResource PanelRaisedBrush}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="3"
                                Visibility="{Binding Figure.SelectedGuideVisibility}">
                            <StackPanel>
                                <TextBlock Text="{Binding Figure.SelectedGuide.OrientationDisplayName}" FontWeight="SemiBold" />
                                <TextBlock Margin="0,7,0,3" Text="位置 / 画布 px" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                <TextBox IsEnabled="{Binding Figure.SelectedGuide.CanEditPosition}"
                                         Text="{Binding Figure.SelectedGuide.Position, UpdateSourceTrigger=PropertyChanged}" />
                                <CheckBox Margin="0,7,0,0" IsChecked="{Binding Figure.SelectedGuide.IsLocked}" Content="锁定参考线" />
                                <Button Margin="0,7,0,0" Command="{Binding Figure.RemoveSelectedGuideCommand}" Content="删除参考线" />
                            </StackPanel>
                        </Border>

                        <Grid Margin="0,20,0,8">
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="科研标注层" />
                            <TextBlock HorizontalAlignment="Right" Text="{Binding Figure.AnnotationCountText}" Foreground="{StaticResource MutedBrush}" FontSize="10" />
                        </Grid>
                        <UniformGrid Columns="2" Rows="3">
                            <Button Margin="0,0,5,5" Command="{Binding Figure.AddTextAnnotationCommand}" Content="＋ 文字" />
                            <Button Margin="0,0,0,5" Command="{Binding Figure.AddArrowAnnotationCommand}" Content="＋ 箭头" />
                            <Button Margin="0,0,5,5" Command="{Binding Figure.AddLineAnnotationCommand}" Content="＋ 直线" />
                            <Button Margin="0,0,0,5" Command="{Binding Figure.AddRectangleAnnotationCommand}" Content="＋ 矩形" />
                            <Button Margin="0,0,5,0" Command="{Binding Figure.AddEllipseAnnotationCommand}" Content="＋ 椭圆" />
                        </UniformGrid>
                        <ListBox Margin="0,8,0,0"
                                 MinHeight="80"
                                 MaxHeight="180"
                                 Background="{StaticResource PanelRaisedBrush}"
                                 BorderBrush="{StaticResource BorderBrush}"
                                 BorderThickness="1"
                                 ItemsSource="{Binding Figure.Annotations}"
                                 SelectedItem="{Binding Figure.SelectedAnnotation}">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Padding" Value="0" />
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="7,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <CheckBox IsChecked="{Binding IsVisible}" ToolTip="显示/隐藏标注" />
                                        <StackPanel Grid.Column="1" Margin="8,0">
                                            <TextBlock Text="{Binding Summary}" FontSize="10" TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{Binding Color}" Foreground="{StaticResource MutedBrush}" FontSize="8" />
                                        </StackPanel>
                                        <CheckBox Grid.Column="2" IsChecked="{Binding IsLocked}" Content="锁" FontSize="9" />
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <UniformGrid Margin="0,8,0,0" Columns="3">
                            <Button Margin="0,0,5,0" Padding="7" Command="{Binding Figure.MoveAnnotationUpCommand}" Content="上移" />
                            <Button Margin="0,0,5,0" Padding="7" Command="{Binding Figure.MoveAnnotationDownCommand}" Content="下移" />
                            <Button Padding="7" Command="{Binding Figure.RemoveSelectedAnnotationCommand}" Content="移除" />
                        </UniformGrid>

                        <Border Margin="0,10,0,0"
                                Padding="10"
                                Background="{StaticResource PanelRaisedBrush}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Visibility="{Binding Figure.SelectedAnnotationVisibility}">
                            <StackPanel>
                                <TextBlock Text="{Binding Figure.SelectedAnnotation.KindDisplayName, FallbackValue=未选择标注}" FontWeight="SemiBold" />
                                <TextBox Margin="0,8,0,0"
                                         Visibility="{Binding Figure.SelectedAnnotation.TextVisibility}"
                                         Text="{Binding Figure.SelectedAnnotation.Text, UpdateSourceTrigger=PropertyChanged}" />
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="X" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding Figure.SelectedAnnotation.X, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Y" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding Figure.SelectedAnnotation.Y, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </Grid>
                                <Grid Margin="0,8,0,0" Visibility="{Binding Figure.SelectedAnnotation.EndpointVisibility}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Figure.SelectedAnnotation.EndXLabel}" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding Figure.SelectedAnnotation.EndX, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="{Binding Figure.SelectedAnnotation.EndYLabel}" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding Figure.SelectedAnnotation.EndY, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </Grid>
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="颜色" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <ComboBox Margin="0,4,0,0"
                                                  IsEditable="True"
                                                  Text="{Binding Figure.SelectedAnnotation.Color, UpdateSourceTrigger=PropertyChanged}">
                                            <ComboBoxItem Content="#FF111111" />
                                            <ComboBoxItem Content="#FFFFFFFF" />
                                            <ComboBoxItem Content="#FFE53935" />
                                            <ComboBoxItem Content="#FF1E88E5" />
                                            <ComboBoxItem Content="#FFFFB300" />
                                        </ComboBox>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" Visibility="{Binding Figure.SelectedAnnotation.TextVisibility}">
                                        <TextBlock Text="字号 / PT" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding Figure.SelectedAnnotation.FontSizePt, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" Visibility="{Binding Figure.SelectedAnnotation.StrokeVisibility}">
                                        <TextBlock Text="线宽 / PT" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding Figure.SelectedAnnotation.StrokeWidthPt, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </Grid>
                                <CheckBox Margin="0,8,0,0"
                                          Visibility="{Binding Figure.SelectedAnnotation.TextVisibility}"
                                          IsChecked="{Binding Figure.SelectedAnnotation.IsBold}"
                                          Content="粗体" />
                                <TextBlock Margin="0,8,0,0"
                                           Text="{Binding Figure.SelectedAnnotation.ValidationMessage}"
                                           Foreground="#80DDEA"
                                           FontSize="9"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <Grid Margin="0,20,0,8">
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="投稿导出预设" />
                            <TextBlock HorizontalAlignment="Right" Text="{Binding ExportProfileSummary}" Foreground="{StaticResource MutedBrush}" FontSize="10" />
                        </Grid>
                        <ListBox MinHeight="90"
                                 MaxHeight="180"
                                 ItemsSource="{Binding ExportProfiles}"
                                 SelectedItem="{Binding SelectedExportProfile}"
                                 Background="{StaticResource PanelRaisedBrush}"
                                 BorderBrush="{StaticResource BorderBrush}"
                                 BorderThickness="1">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="7,5">
                                        <TextBlock Text="{Binding Name}" FontSize="10" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{Binding Summary}" Foreground="{StaticResource MutedBrush}" FontSize="8" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <UniformGrid Margin="0,7,0,0" Columns="3">
                            <Button Margin="0,0,5,0" Padding="6" Command="{Binding AddExportProfileCommand}" Content="新增" />
                            <Button Margin="0,0,5,0" Padding="6" Command="{Binding RemoveSelectedExportProfileCommand}" Content="移除" />
                            <Button Padding="6" Command="{Binding ResetExportProfilesCommand}" Content="恢复默认" />
                        </UniformGrid>
                        <Border Margin="0,8,0,0" Padding="10" Background="{StaticResource PanelRaisedBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="名称" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                <TextBox Margin="0,4,0,0" Text="{Binding SelectedExportProfile.Name, UpdateSourceTrigger=PropertyChanged}" />
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="格式" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <ComboBox Margin="0,4,0,0" IsEditable="True" Text="{Binding SelectedExportProfile.Format, UpdateSourceTrigger=PropertyChanged}">
                                            <ComboBoxItem Content="tiff" />
                                            <ComboBoxItem Content="png" />
                                            <ComboBoxItem Content="jpg" />
                                            <ComboBoxItem Content="pdf" />
                                            <ComboBoxItem Content="svg" />
                                        </ComboBox>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="位深" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <ComboBox Margin="0,4,0,0" IsEditable="True" Text="{Binding SelectedExportProfile.BitDepth, UpdateSourceTrigger=PropertyChanged}">
                                            <ComboBoxItem Content="8" />
                                            <ComboBoxItem Content="16" />
                                        </ComboBox>
                                    </StackPanel>
                                </Grid>
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="DPI" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding SelectedExportProfile.Dpi, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="缩放倍数" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding SelectedExportProfile.Scale, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </Grid>
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="目标宽 / px（可空）" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding SelectedExportProfile.WidthPixels, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="目标高 / px（可空）" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                                        <TextBox Margin="0,4,0,0" Text="{Binding SelectedExportProfile.HeightPixels, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                </Grid>
                                <CheckBox Margin="0,8,0,0" IsChecked="{Binding SelectedExportProfile.WriteProvenance}" Content="生成溯源 JSON 与 HTML 报告" />
                                <TextBlock Margin="0,6,0,0" Text="{Binding SelectedExportProfile.ValidationMessage}" Foreground="#80DDEA" FontSize="8" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <TextBlock Margin="0,18,0,8" Style="{StaticResource SectionTitleStyle}" Text="智能辅助 · 可解释 / 可撤销" />
                        <Border Padding="10" Background="#171A21" BorderBrush="#66543A" BorderThickness="1" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="使用确定性规则辅助布局、样式和科研诚信审查；不执行生成式填充、克隆、局部擦除或对象移除。"
                                           Foreground="#E8C989" FontSize="9" TextWrapping="Wrap" />
                                <Button Margin="0,9,0,0"
                                        Command="{Binding ApplySmartLayoutCommand}"
                                        Content="推荐并应用布局" />
                                <Button Margin="0,6,0,0"
                                        Command="{Binding HarmonizeFigureStyleCommand}"
                                        Content="协调全局样式" />
                                <Button Margin="0,6,0,0"
                                        Command="{Binding RunAssistedFigureReviewCommand}"
                                        Style="{StaticResource AccentButtonStyle}"
                                        Content="运行样式 + 科研诚信辅助审查" />
                                <TextBlock Margin="0,7,0,0"
                                           Text="{Binding SmartAssistStatusText}"
                                           Foreground="#D9C29A"
                                           FontSize="8"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <TextBlock Margin="0,18,0,8" Style="{StaticResource SectionTitleStyle}" Text="Figure QC · 投稿前检查" />
                        <Border Padding="10" Background="#121E27" BorderBrush="#315365" BorderThickness="1" CornerRadius="4">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding FigureQcStatusText}" Foreground="#DCE7F0" FontSize="10" FontWeight="SemiBold" TextWrapping="Wrap" />
                                        <TextBlock Margin="0,3,0,0" Text="{Binding FigureQcCountText}" Foreground="{StaticResource MutedBrush}" FontSize="8" />
                                    </StackPanel>
                                    <Button Grid.Column="1" Margin="8,0,0,0" Padding="9,5" Command="{Binding RunFigureQcCommand}" Content="重新检查" />
                                </Grid>
                                <ListBox Margin="0,9,0,0"
                                         MinHeight="54"
                                         MaxHeight="230"
                                         Background="#0D151C"
                                         BorderBrush="{StaticResource BorderBrush}"
                                         BorderThickness="1"
                                         ItemsSource="{Binding FigureQcIssues}"
                                         SelectedItem="{Binding SelectedFigureQcIssue}"
                                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                            <Setter Property="Padding" Value="0" />
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Margin="5,4" Padding="7" BorderBrush="{Binding SeverityBrush}" BorderThickness="3,0,0,0">
                                                <StackPanel>
                                                    <Grid>
                                                        <TextBlock Text="{Binding SeverityText}" Foreground="{Binding SeverityBrush}" FontSize="8" FontWeight="Bold" />
                                                        <TextBlock HorizontalAlignment="Right" Text="{Binding TargetText}" Foreground="{StaticResource MutedBrush}" FontSize="8" />
                                                    </Grid>
                                                    <TextBlock Margin="0,3,0,0" Text="{Binding Message}" FontSize="9" TextWrapping="Wrap" />
                                                    <TextBlock Margin="0,3,0,0" Text="{Binding SuggestedAction}" Foreground="{StaticResource MutedBrush}" FontSize="8" TextWrapping="Wrap" />
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                                <Button Margin="0,8,0,0" Command="{Binding NavigateToSelectedQcIssueCommand}" Content="定位选中问题" />
                            </StackPanel>
                        </Border>

                        <Button Margin="0,18,0,0" Command="{Binding AddCurrentCropToFigureCommand}" Content="加入当前裁剪" />
                        <Button Margin="0,8,0,0" Command="{Binding ExportFigureCommand}" Style="{StaticResource AccentButtonStyle}" Content="导出整张拼版" />
                        <Button Margin="0,8,0,0" Command="{Binding ExportFigureVariantsCommand}" Content="批量导出投稿版本（主图/补充图/缩略图）" />
                        <TextBlock Margin="0,8,0,0"
                                   Text="最终组图从原始文件重新裁剪并渲染；可导出 TIFF、可编辑 PDF 或 SVG。批量投稿版本会生成主图、补充图、缩略图及各自溯源报告。"
                                   Foreground="{StaticResource MutedBrush}"
                                   FontSize="9"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                    </Grid>
                    </ScrollViewer>
                    <ScrollViewer x:Name="LayersScrollViewer" Grid.Row="1" Visibility="{Binding LayersTabVisibility}" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" CanContentScroll="False">
                        <StackPanel Margin="16,16,16,24">
                            <TextBlock Text="图层" FontSize="15" FontWeight="SemiBold" />
                            <TextBlock Margin="0,4,0,0" Text="当前工作区的图像、测量、标注与参考线统一在这里选择、显隐、锁定和删除。" Foreground="{StaticResource MutedBrush}" FontSize="10" TextWrapping="Wrap" />
                            <Button Margin="0,10,0,0" Command="{Binding DeleteSelectionCommand}" Content="删除选中项 · Delete" />
                            <Border Margin="0,14,0,0" Padding="9" Background="{StaticResource PanelRaisedBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Visibility="{Binding CropWorkspaceVisibility}">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="测量图层" FontWeight="SemiBold" />
                                        <TextBlock Grid.Column="1" Text="{Binding SelectedSource.MeasurementCountText}" Foreground="{StaticResource MutedBrush}" FontSize="10" />
                                    </Grid>
                                    <ListBox Margin="0,9,0,0"
                                             MinHeight="150"
                                             MaxHeight="420"
                                             Background="{StaticResource PanelBrush}"
                                             BorderBrush="{StaticResource BorderBrush}"
                                             BorderThickness="1"
                                             ItemsSource="{Binding SelectedSource.Measurements}"
                                             SelectedItem="{Binding SelectedSource.SelectedMeasurement}"
                                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                        <ListBox.ItemContainerStyle>
                                            <Style TargetType="ListBoxItem">
                                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                <Setter Property="Padding" Value="0" />
                                            </Style>
                                        </ListBox.ItemContainerStyle>
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="7,7">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <CheckBox IsChecked="{Binding IsVisible}" ToolTip="显示/隐藏测量图层" />
                                                    <StackPanel Grid.Column="1" Margin="8,0">
                                                        <TextBlock Text="{Binding LayerStateText}" FontSize="11" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                                        <TextBlock Text="{Binding ValueText}" Foreground="{StaticResource MutedBrush}" FontSize="9" TextTrimming="CharacterEllipsis" />
                                                    </StackPanel>
                                                    <CheckBox Grid.Column="2" IsChecked="{Binding IsLocked}" Content="锁" FontSize="9" ToolTip="锁定测量图层" />
                                                </Grid>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                    <TextBlock Margin="0,7,0,0" Text="单击画布图形或列表均可选择；锁定后仍可查看，但不能移动、缩放或删除。" Foreground="{StaticResource MutedBrush}" FontSize="8" TextWrapping="Wrap" />
                                </StackPanel>
                            </Border>
                            <Border Margin="0,14,0,0" Padding="9" Background="{StaticResource PanelRaisedBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Visibility="{Binding FigureWorkspaceVisibility}">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="拼版面板" FontWeight="SemiBold" />
                                        <TextBlock Grid.Column="1" Text="{Binding Figure.PanelCountText}" Foreground="{StaticResource MutedBrush}" FontSize="10" />
                                    </Grid>
                                    <ListBox Margin="0,9,0,0" MinHeight="140" MaxHeight="360" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" ItemsSource="{Binding Figure.Panels}" SelectionMode="Extended" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                        <ListBox.ItemContainerStyle>
                                            <Style TargetType="ListBoxItem">
                                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                <Setter Property="Padding" Value="0" />
                                                <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                                            </Style>
                                        </ListBox.ItemContainerStyle>
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="7,7">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <CheckBox IsChecked="{Binding IsVisible}" ToolTip="显示/隐藏图层" />
                                                    <StackPanel Grid.Column="1" Margin="8,0">
                                                        <TextBlock FontSize="11" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"><Run Text="{Binding Label, Mode=OneWay}" /><Run Text=" · " /><Run Text="{Binding RoleDisplayName, Mode=OneWay}" /></TextBlock>
                                                        <TextBlock Text="{Binding Source.DisplayName}" Foreground="{StaticResource MutedBrush}" FontSize="9" TextTrimming="CharacterEllipsis" />
                                                    </StackPanel>
                                                    <CheckBox Grid.Column="2" IsChecked="{Binding IsLocked}" Content="锁" FontSize="9" ToolTip="锁定图层" />
                                                </Grid>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                    <UniformGrid Margin="0,8,0,0" Columns="3">
                                        <Button Margin="0,0,5,0" Padding="7" Command="{Binding Figure.MoveLayerUpCommand}" Content="上移" />
                                        <Button Margin="0,0,5,0" Padding="7" Command="{Binding Figure.MoveLayerDownCommand}" Content="下移" />
                                        <Button Padding="7" Command="{Binding Figure.RemoveSelectedCommand}" Content="移除" />
                                    </UniformGrid>
                                    <UniformGrid Margin="0,6,0,0" Columns="2">
                                        <Button Margin="0,0,5,0" Padding="6" Command="{Binding Figure.SelectAllPanelsCommand}" Content="全选面板" />
                                        <Button Padding="6" Command="{Binding Figure.ClearPanelSelectionCommand}" Content="取消选择" />
                                    </UniformGrid>
                                </StackPanel>
                            </Border>
                            <Border Margin="0,12,0,0" Padding="9" Background="{StaticResource PanelRaisedBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Visibility="{Binding FigureWorkspaceVisibility}">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="标注图层" FontWeight="SemiBold" />
                                        <TextBlock Grid.Column="1" Text="{Binding Figure.AnnotationCountText}" Foreground="{StaticResource MutedBrush}" FontSize="10" />
                                    </Grid>
                                    <ListBox Margin="0,9,0,0"
                                             MinHeight="110"
                                             MaxHeight="300"
                                             Background="{StaticResource PanelBrush}"
                                             BorderBrush="{StaticResource BorderBrush}"
                                             BorderThickness="1"
                                             ItemsSource="{Binding Figure.Annotations}"
                                             SelectedItem="{Binding Figure.SelectedAnnotation}"
                                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                        <ListBox.ItemContainerStyle>
                                            <Style TargetType="ListBoxItem">
                                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                <Setter Property="Padding" Value="0" />
                                            </Style>
                                        </ListBox.ItemContainerStyle>
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="7,7">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <CheckBox IsChecked="{Binding IsVisible}" ToolTip="显示/隐藏标注图层" />
                                                    <StackPanel Grid.Column="1" Margin="8,0">
                                                        <TextBlock Text="{Binding KindDisplayName}" FontSize="11" FontWeight="SemiBold" />
                                                        <TextBlock Text="{Binding Text}" Foreground="{StaticResource MutedBrush}" FontSize="9" TextTrimming="CharacterEllipsis" />
                                                    </StackPanel>
                                                    <CheckBox Grid.Column="2" IsChecked="{Binding IsLocked}" Content="锁" FontSize="9" ToolTip="锁定标注图层" />
                                                </Grid>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                    <UniformGrid Margin="0,8,0,0" Columns="3">
                                        <Button Margin="0,0,5,0" Padding="7" Command="{Binding Figure.MoveAnnotationUpCommand}" Content="上移" />
                                        <Button Margin="0,0,5,0" Padding="7" Command="{Binding Figure.MoveAnnotationDownCommand}" Content="下移" />
                                        <Button Padding="7" Command="{Binding Figure.RemoveSelectedAnnotationCommand}" Content="删除" />
                                    </UniformGrid>
                                </StackPanel>
                            </Border>
                            <Border Margin="0,12,0,0" Padding="9" Background="{StaticResource PanelRaisedBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Visibility="{Binding FigureWorkspaceVisibility}">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="参考线图层" FontWeight="SemiBold" />
                                        <TextBlock Grid.Column="1" Text="{Binding Figure.GuideCountText}" Foreground="{StaticResource MutedBrush}" FontSize="10" />
                                    </Grid>
                                    <ListBox Margin="0,9,0,0"
                                             MinHeight="80"
                                             MaxHeight="220"
                                             Background="{StaticResource PanelBrush}"
                                             BorderBrush="{StaticResource BorderBrush}"
                                             BorderThickness="1"
                                             ItemsSource="{Binding Figure.Guides}"
                                             SelectedItem="{Binding Figure.SelectedGuide}">
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="7,6">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                     <TextBlock Text="{Binding Summary}" />
                                                    <CheckBox Grid.Column="1" IsChecked="{Binding IsLocked}" Content="锁" FontSize="9" />
                                                </Grid>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                    <Button Margin="0,8,0,0" Command="{Binding Figure.RemoveSelectedGuideCommand}" Content="删除选中参考线" />
                                </StackPanel>
                            </Border>
                            <Border Margin="0,12,0,0" Padding="9" Background="#10212A" BorderBrush="#245A68" BorderThickness="1" CornerRadius="3">
                                <StackPanel>
                                    <TextBlock Text="图层说明" Foreground="#8CE5F4" FontSize="10" FontWeight="SemiBold" />
                                    <TextBlock Margin="0,4,0,0" Text="一次只激活一种图层类型；Delete 删除当前选中对象。眼睛控制显示，锁定会阻止拖动、改大小和删除。" Foreground="{StaticResource MutedBrush}" FontSize="9" TextWrapping="Wrap" />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="3" Background="#10161E" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid Margin="12,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="●" Foreground="#62D49C" FontSize="9" />
                    <TextBlock Margin="6,0,0,0" Text="{Binding StatusMessage}" Foreground="#AEB9C6" FontSize="10" />
                    <TextBlock Margin="14,0,0,0" Text="{Binding LastError}" Foreground="#F28A8A" FontSize="10" TextTrimming="CharacterEllipsis" MaxWidth="560" ToolTip="{Binding LastError}" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Margin="0,0,14,0" Text="{Binding AutosaveStatusText}" Foreground="#62D49C" FontSize="9" />
                    <TextBlock Text="{Binding HistoryStatusText}" Foreground="#7FA8B5" FontSize="9" />
                    <TextBlock Margin="14,0,0,0" Text="本地模式 · 无网络上传" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
